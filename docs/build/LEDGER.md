# kube-agents — Build Ledger

**This file is the harness's memory across sessions and days.** Every harness run reads it
first to learn where the build is, then updates it before stopping. It is the single source of
truth for build progress — not chat history, not git log alone.

- **Source of truth for _what_ to build:** `docs/design/` (01–08). Never contradict a spec; if a
  spec is genuinely silent, pick the simplest option consistent with the invariants, implement it,
  and record it under **Decisions & deviations** below.
- **Source of truth for _how_ to build:** `docs/design/07-implementation-roadmap.md` (phases,
  acceptance, verification loop §5) + `.claude/harness/invariants.md` (the pre-merge gate).
- **How the loop works:** `.claude/skills/harness-run/SKILL.md`.

---

## Status

| Field                                                                                                | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Current phase                                                                                        | **Phase 7 — Cloud-agnostic seams** (07 §2, the **final** phase; reduce GKE coupling per 01 §6 by exercising the already-unopinionated seams: generate **Terraform HCL** as well as KCC YAML, actuate via a **second CI/CD** (CircleCI) alongside the GH-Actions `apply.yml`, and abstract **observability** behind provider-neutral seams; Accept — a second target (**vanilla Kind**, non-GKE) passes the Phase 1–3 acceptance on core concepts; breakdown: `docs/build/phase-7.md`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Current task                                                                                         | **Phase 7 COMPLETE — final roadmap phase; gate green; PR opened + auto-merge pending.** All 6 tasks done: **P7-T1** (`a92abc7`) matched KCC (`cluster-a`) + Terraform HCL (`cluster-b`) exemplars + `iac-parity.py`; **P7-T2** (`0ae06d7`) CircleCI second pipeline + `circleci-parity.py`; **P7-T3** (`0bfc908`) env-driven OTLP endpoint + backend-selectable obs skill (GKE/GCP default); **P7-T4 + P7-T5** (`0155e09`) `verify-phase7.sh kind-kube-agents-dev` **exit 0** — Section A T1–T3 hermetic validators, Section B vanilla-target core-concept acceptance (node kubeletVersion `v1.31.2`, no `-gke` suffix; read-only SAR, GitOps-PR-only, isolation, `(tier,scope)` webhook, VAP attenuation, deterministic routing `inference_calls==0`, explicit no-GKE static assertion on the mechanism path), Section C full regression (`verify-phase6.sh` → chaos C1–C4 + `verify-phase{2,3,4,5}.sh` + 03 §11 + goldens + `go test ./...`); **P7-T6** docs (INSTALL Phase 7 + ToC, this LEDGER, memory) + PR → `main` on `fork`. **07 §3 Definition of Done (9 items)** recorded below with per-item evidence/deferral. Deferred-not-faked: real EKS/AKS cloud identity + live `terraform apply`/cross-cloud pipeline (D1/D2); `terraform validate`/`circleci config validate` CLI enforcement (CLIs absent → structural+semantic proven hermetically, same pattern as Calico egress); live non-GCP observability backend (D3).                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Overall                                                                                              | ✅ Phase 0 **merged**; ✅ Phase 1 **merged** ([#3](https://github.com/adamparco/kube-agents/pull/3), `a9ffe98`); ✅ Phase 2 **merged** ([#4](https://github.com/adamparco/kube-agents/pull/4), `0b418de`); ✅ Phase 3 **merged** ([#5](https://github.com/adamparco/kube-agents/pull/5), `405d31b`) — 14/14 tasks, Kind P3-K1..K7 (30 checks) green incl. A1 placement + A2 isolation, verify-phase2 regression green; ✅ Phase 4 **merged** ([#6](https://github.com/adamparco/kube-agents/pull/6), `17dc452`) — 12/12 tasks, verify-phase4.sh exit 0 (Acc a–e hermetic + live-Kind regression), verify-phase3 not regressed; ✅ Phase 5 **merged** ([#7](https://github.com/adamparco/kube-agents/pull/7), `ac57a17`) — 11/11 tasks, `verify-phase5.sh` exit 0 (review-gate scorer blocks unmitigated high; all-tier egress + Calico enforcement PROVEN; `readOnlyRootFilesystem`+PSS+VAP with live admission dry-run PROVEN; end-to-end attribution), regression green; ✅ Phase 6 **merged** ([#8](https://github.com/adamparco/kube-agents/pull/8), `713f643`) — failure-isolation & resilience (05 §8 chaos C1–C4 + unopinionated actuation; `verify-phase6.sh` exit 0; full regression green); 🟢 **Phase 7 — Cloud-agnostic seams — gate green, PR [#9](https://github.com/adamparco/kube-agents/pull/9) opened (auto-merge pending)** — the **final** roadmap phase; 6/6 tasks P7-T1..T6; `verify-phase7.sh` exit 0 (IaC + CircleCI + obs-seam parity, vanilla non-GKE core-concept acceptance, full regression); 07 §3 Definition of Done recorded (9 items); branch `phase-7-cloud-agnostic-seams`; breakdown `docs/build/phase-7.md`. **On merge, the entire roadmap build (Phases 0–7) is complete.**                                                                                                                                                                |
| Autonomy                                                                                             | **Fully autonomous** (advance across phase boundaries; halt only on hard blocker or failed load-bearing suite)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Verification targets                                                                                 | **Kind (inner loop)** + **scratch GKE (identity/cloud criteria)**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Last updated                                                                                         | 2026-07-24 — **Phase 7 — Cloud-agnostic seams — COMPLETE (final roadmap phase); gate green; PR opened, auto-merge pending.** All three seams realized while keeping GKE/GCP the zero-config default: **P7-T1** (`a92abc7`) matched KCC (`cluster-a`) + Terraform HCL (`cluster-b`, `iac.format: terraform`) exemplars, `iac-parity.py` proves HCL structural validity + KCC↔HCL semantic equivalence + `apply.yml` per-format dispatch (14 facts); **P7-T2** (`0ae06d7`) CircleCI `.circleci/config.yml` second reference pipeline, `circleci-parity.py` proves dispatch parity vs `apply.yml` + no agent-held credential (invariant 2 unaffected, D4); **P7-T3** (`0bfc908`) OTLP endpoint moved to `OTEL_EXPORTER_OTLP_ENDPOINT` (entrypoint-resolved, GKE default) + obs skill backend behind `KUBEAGENTS_OBS_BACKEND`/`OBS_*_BASE_URL` (gcp default), `observability-seam.py`+`otel-endpoint.sh` prove env-override + unset-default-unchanged + fail-loud-on-missing; **P7-T4+P7-T5** (`0155e09`) `verify-phase7.sh kind-kube-agents-dev` **exit 0** — vanilla non-GKE target (kubeletVersion `v1.31.2`), core concepts hold, no-GKE static assertion on the mechanism path, full regression green. **P7-T6** docs (INSTALL Phase 7 + ToC, this LEDGER, memory) + PR → `main` on `fork`; **07 §3 Definition of Done (9 items)** recorded below with per-item evidence/deferral. Deferred-not-faked: real EKS/AKS cloud identity + live `terraform apply`/cross-cloud pipeline (D1/D2); `terraform validate`/`circleci config validate` CLI enforcement (CLIs absent → hermetic structural+semantic, same pattern as Calico for egress); live non-GCP observability backend (D3); plus the carried scratch-GKE V-G checks and 08 §5 deferred hardening. Invariants unaffected (seam work adds no write path). **On merge, the entire roadmap build (Phases 0–7) is complete.** |
| Last updated by                                                                                      | harness-run (Phase 7 — cloud-agnostic seams — complete; `verify-phase7.sh` exit 0; 07 §3 Definition of Done recorded; PR → `main` on fork, auto-merge pending)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Merge policy (set by user 2026-07-24):** phase PRs are **auto-merged once the pre-merge gate is    |
| green** (invariants + load-bearing suites passing on Kind), then the build continues — no wait for   |
| manual merge. Auto-merge is gated, not blind: any halt condition below blocks the merge and surfaces |
| instead. **Base/target = `main`** — PR #1 consolidated design + Phase 0 onto `main` (`11d95fd`), so  |
| future phase branches base on `main` and PR → `main`. (Product runtime GitOps review is unaffected — |
| that is a separate, RBAC/VAP-enforced property of the built system.)                                 |

**Load-bearing halt conditions (stop and surface, do NOT merge or auto-advance):**

1. A **security negative test** (03 §11) fails — read-only, per-tier scope, attenuation, no break-glass.
2. A **failure-isolation chaos test** (05 §8) fails.
3. An **invariant** (`.claude/harness/invariants.md`) would be violated by the change.
4. A destructive test is about to run anywhere other than **Kind** or a **scratch GKE** cluster.
5. A spec conflict with no simplest-option resolution.

---

## Phase progress

Phases and acceptance criteria are defined in `docs/design/07-implementation-roadmap.md` §2.
Detailed task breakdowns live in `docs/build/phase-<N>.md` (created when the phase is entered).

| Phase | Title                                  | Status              | PR                                                    | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ----- | -------------------------------------- | ------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0     | Foundations                            | ✅ merged to `main` | [#2](https://github.com/adamparco/kube-agents/pull/2) | A1–A4 green; pre-PR gate (4 fixes); PR #2 `8a31c76`; **on `main` via PR [#1](https://github.com/adamparco/kube-agents/pull/1) `11d95fd`** (design + Phase 0)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 1     | Read-only Platform Agent + GitOps loop | ✅ merged           | [#3](https://github.com/adamparco/kube-agents/pull/3) | 12/12 tasks; A1–A4 green on Kind + scratch cloud; load-bearing VAP suite (03 §11) PASS; GSA reconciled viewer-only; merged `a9ffe98` (all 6 CI checks green)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2     | Cluster Admin Agent + cascade          | ✅ merged           | [#4](https://github.com/adamparco/kube-agents/pull/4) | 24/25 tasks; CRD hard-rename → generic `Agent` + shared `agentindex`; cluster-admin image/CR/read-only identity; **kage-router** (deterministic slash/@handle, authz-before-dispatch); spoke bootstrap waves; per-tier egress; **F4 cascade skill**. Kind V-K0..V-K11 GREEN; V-G1..G4 pending scratch-GKE; merged `0b418de` (all 6 CI checks green)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 3     | Developer Team Agent + isolation proof | ✅ merged           | [#5](https://github.com/adamparco/kube-agents/pull/5) | 14/14 tasks. Track A (T1–T7): dev-team image/otel/webhook-placement-clause/persona+Dockerfile/namespaced-identity+CR+VAP/isolation-manifests/`propose-developer-team` cascade. Track B (T8–T12): index-assisted routing, NL `Resolve`/`Infer` split, candidate-validity two-barrier, thread affinity, audit attribution + `Replier` seam. T13 `verify-phase3.sh` (30 checks P3-K1..K7) + go-test + verify-phase2 regression exit 0; A1 placement escape REJECTED after refreshing a stale Kind image. Merged `405d31b` (all 6 CI checks green).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 4     | Coordination & knowledge               | ✅ merged           | [#6](https://github.com/adamparco/kube-agents/pull/6) | 12/12 tasks P4-T1..T12 (design judge-panel → verifiability-first + invariant-first grafts). Track S: inject-seam auth/loopback (T1) + `kind` discriminator (T2). Track A: per-tier watcher scoping + fail-closed validate (T3), eventingress cloud leg (T4). Track B: `read-knowledge` sparse checkout (T5), escalation round-trip (T6). Track C: `--dry-run` (T7), Platform drift SOP (T8), per-tier heartbeat SOPs (T9). Seed knowledge (T10). `verify-phase4.sh` exit 0 (Acc a–e hermetic + live-Kind regression: 03 §11 attenuation, dev-team read-only SAR under trigger, 08 §7 controller-mints-no-RBAC); `go test ./...` 10 pkgs ok; verify-phase3 not regressed. 05 §8 chaos N-A → Phase 6. Merged `17dc452` (all load-bearing CI green; only red is the benign Auto-Request-Review fork bot).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 5     | Security gate & hardening              | ✅ merged           | [#7](https://github.com/adamparco/kube-agents/pull/7) | **11/11 tasks P5-T1..T11 done; `verify-phase5.sh` exit 0 — ALL CHECKS PASSED.** Track R (review-gate CI, 06 §7): P5-T1 `severity` on finding schema, P5-T2 waiver/fingerprint format, P5-T3 hermetic scorer (unmitigated high/critical → block; 33+7 unit tests), P5-T4 `review-gate.yml` (path-glob+heartbeat triggers; headless detector→scorer, skips w/o `ANTHROPIC_API_KEY`). Track E: P5-T5 platform-tier netpol (net-new; all 3 tiers pure allowlist, no 0.0.0.0/0), P5-T6 **Calico enforcement PROVEN** (off-allowlist DENIED / on-allowlist ALLOWED). Track H: P5-T7 `readOnlyRootFilesystem` all containers + regen goldens, P5-T8 PSS `restricted` label + `vap-agent-pod-hardening` VAP — **live VAP dry-run PROVEN** (rejects un-hardened agent pod, admits hardened, non-agent untouched). Track T: P5-T9 requester+trace router→inject→session→PR `Requested-by:`/`Trace-Id:` trailers + audit ties `Sender`↔`TraceID`. P5-T10 `verify-phase5.sh` (Acc a–d + live VAP/egress + regress). P5-T11 docs (INSTALL Phase 5 + ToC, LEDGER, memory) + PR→main. Accept (a) unmitigated HIGH BLOCKED ✅; (b) off-allowlist egress DENIED ✅ (Calico); (c) every pod hardened+enforced ✅; (d) every mutation attributable ✅. Regression: 03 §11 negatives + verify-phase{2,3,4}.sh + `go test ./...` all green. 05 §8 chaos → Phase 6 (N-A). Deferred hardening (08 §5): cross-object webhook, gVisor sandbox, per-request user down-scoping; L7 hostname egress proxy. **Merged `ac57a17`** — all substantive CI green (build, Controller Tests, Security review gate, docs, actionlint, prettier, validate); only red the benign Auto-Request-Review fork bot.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 6     | Failure-isolation & resilience         | ✅ merged           | [#8](https://github.com/adamparco/kube-agents/pull/8) | **Merged `713f643` 2026-07-24 (all 6 CI checks green; benign Auto-Request-Review fork bot the only red).** Gate green: `chaos-suite.sh` + `verify-phase6.sh` **exit 0** on `kind-kube-agents-dev`. **C1** controller-down: a running pod stays Ready, the deleted **real** agent Deployment is NOT recreated (no reconciles while down), resumes on scale-up [Accept b]; **C2** controller relaunches the real agent Deployment (owned by CR `cluster-admin-cluster-a`) + a deleted pod [Accept c]; **C3** cluster-admin-down → developer-team pod UID-stable + Ready, **no cascade**, cluster-admin relaunched [Accept b]; **C4** hub-down → spoke-workload survives, decoupled (own ReplicaSet, no hub ownerRef), no Config Sync/Connector/Argo/Flux required [Accept a]. Fixtures (D1): real CR/controller for reconcile-behaviour, hardened tier-labeled `pause` stand-ins for pod-continuity. Full regression (verify-phase{2,3,4,5} + 03 §11 + goldens + `go test ./...`) green. Finding+fix: stale `v0.1.0` controller predated Phase-5 hardening → rebuilt + loaded + redeployed hardened controller (Decisions). Deferred not faked (D3): real spoke agent-reasoning-pause under real hub loss → scratch-GKE. Breakdown `docs/build/phase-6.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 7     | Cloud-agnostic seams                   | 🟢 gate green (PR)  | [#9](https://github.com/adamparco/kube-agents/pull/9) | **6/6 tasks P7-T1..T6 done; `verify-phase7.sh kind-kube-agents-dev` exit 0 — ALL CHECKS PASSED; PR → `main` on fork, auto-merge pending (final roadmap phase).** Reduces GKE coupling (01 §6) by realizing three already-unopinionated seams while keeping GKE/GCP the zero-config default. **P7-T1** (`a92abc7`): matched KCC (`cluster-a`) + Terraform HCL (`cluster-b`, `iac.format: terraform`) provisioning exemplars; `iac-parity.py` exit 0 — HCL structurally valid (required `terraform{}`/`google_container_cluster`/`google_container_node_pool` blocks+attrs, balanced braces) **and** semantically equivalent to KCC (location/channel/machine/count/network); `apply.yml` dispatches `.tf`→terraform / `.y*ml`→kubectl; bad-HCL negative control fails [Accept a]. **P7-T2** (`0ae06d7`): `.circleci/config.yml` second reference pipeline; `circleci-parity.py` exit 0 — valid `version:2.1` + apply job + workflow filtered to `main`, same KCC/HCL dispatch + per-target least-priv creds as `apply.yml`, **no agent-held write credential** (invariant 2, D4); malformed-config negative control fails [Accept b]. **P7-T3** (`0bfc908`): OTLP endpoint → `OTEL_EXPORTER_OTLP_ENDPOINT` (entrypoint-resolved, exact `gke-managed-otel` default) + obs skill base URLs → `KUBEAGENTS_OBS_BACKEND`/`OBS_*_BASE_URL` (gcp default); `observability-seam.py` (6) + `otel-endpoint.sh` (6) exit 0 — env override → non-`googleapis.com`, unset → unchanged default, non-GCP profile w/ missing URL **fails loudly** (D3). **P7-T4+T5** (`0155e09`): `verify-phase7.sh` Section B vanilla non-GKE target (node kubeletVersion `v1.31.2`, no `-gke` suffix) — read-only SAR, GitOps-PR-only, isolation, `(tier,scope)` webhook, VAP attenuation, deterministic routing `inference_calls==0` (hermetic `TestGateway_ThreadAffinity`), explicit no-GKE static assertion on the mechanism path (VAP/webhook/controller-RBAC/router Go) [Accept c]; Section C full regression `verify-phase6.sh` → chaos C1–C4 + `verify-phase{2,3,4,5}.sh` + 03 §11 + goldens + `go test ./...` not regressed. **P7-T6** docs (INSTALL Phase 7 + ToC, LEDGER, memory) + PR + **07 §3 Definition of Done (9 items)** recorded below. Deferred-not-faked: real EKS/AKS cloud identity (WI→IRSA/AAD) + live `terraform apply`/cross-cloud pipeline (D1/D2); `terraform validate`/`circleci config validate` CLI enforcement (CLIs absent → hermetic, same pattern as Calico); live non-GCP obs backend (D3). |

Legend: ⬜ not started · 🟡 in progress · 🟢 acceptance passing · ✅ merged · 🔴 blocked

---

## Verification log

Append one row per verification run. "Suite" = a phase-acceptance run or a spec Verification
section (02 §10, 03 §11, 04 §9, 05 §8, 06 §10, 08 §7). Keep the most recent result per suite.

| Date       | Phase | Suite                                                                    | Target (Kind/GKE)    | Result    | Evidence (log/PR/commit)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ---------- | ----- | ------------------------------------------------------------------------ | -------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-23 | 0     | A1 — GitOps tree matches 06 §3                                           | local (files)        | ✅ PASS   | `find examples/gitops-repo -type f` matches 06 §3 layout (clusters/<c>/{provisioning,namespaces/<ns>,agents}, fleet, knowledge, policy, .github/workflows)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-23 | 0     | A4 — OKF validator passes on knowledge/                                  | local (files)        | ✅ PASS   | `python3 local-dev/okf-validate.py examples/gitops-repo/knowledge` → exit 0, 2 entries; negative control on a bad entry → exit 1 (not a silent no-op)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-23 | 0     | A3 / 03 §11 — attenuation admission (load-bearing)                       | Kind v1.31.2         | ✅ PASS   | `local-dev/tests/negative-attenuation.sh`: write-verb Role DENIED, wrong-scope ClusterRole DENIED, read-only Role ADMITTED; denials adversarially confirmed from the policy (message match)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-23 | 0     | A2 — overlays policy-clean + SAR isolation                               | Kind v1.31.2         | ✅ PASS   | 3 real read-only overlays admitted by the VAP; SAR: developer-team-agent create secrets→no, list pods in team-x→yes, in kube-system→no                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-23 | 0     | Pre-PR adversarial gate (re-verify + review)                             | Kind v1.31.2         | ✅ PASS   | 22 agents; 17 findings → 5 confirmed → 4 in-scope fixed & re-verified; A1–A4 stayed green. Fixes: VAP read-verb allow-list, anchored destructive guard, negative-test stdin cleanup, OKF link-title. See `phase-0.md` §Pre-PR review gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-23 | 0     | A3 / 03 §11 — impersonate deny + guard (load-bearing)                    | Kind v1.31.2         | ✅ PASS   | New impersonate ClusterRole → DENIED by VAP (message from policy; object never created). Guard refuses 3 prod-lookalike contexts. good-reader `NotFound` after run (leak fixed).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-24 | 1     | 08 §7 — controller mints no RBAC/SA (T4/T5)                              | local (go test)      | ✅ PASS   | `go build/vet/test ./...` green. Golden fixture no longer emits SA/viewer-CRB/explorer-CR+CRB; `config/rbac/role.yaml` has no `clusterroles`/`clusterrolebindings` and `serviceaccounts`→`get;list;watch`. Reconcile unit test asserts no finalizer + no minted RBAC. **Kind verified 2026-07-24:** controller ClusterRole grants no `rbac.authorization.k8s.io` apiGroup at all; `auth can-i` as `platform-agent` SA → reads yes, create/delete/update/escalate no.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-24 | 1     | 06 §1.2/§10 — (tier,scope) cardinality + A4 (T8)                         | local (go test)      | ✅ PASS   | `go build/vet/test ./internal/...` green. New webhook tests: duplicate (platform,project-x) rejected; different projects/tiers coexist; tier change on update rejected; enabled GoogleChat/Slack with empty allowlist rejected, non-empty admitted, disabled admitted. 2 CEL rules regenerated cleanly into the CRD. **Kind verified 2026-07-24:** CRD installed, valid CR admitted; enabled-chat + empty allowlist DENIED by CEL (message match); `tier` patch platform→cluster-admin DENIED (`tier is immutable`). (tier,scope) cardinality is webhook-only → covered by unit tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-24 | 1     | A2 (cloud) / invariant #1 — GSA viewer-only (T9)                         | local (bash)         | ✅ PASS   | `bash -n` clean on all 3 scripts. `get_platform_agent_roles` emits **no** admin role for default/read-only/custom/gke-admin inputs; `gke-admin` coerced→read-only with warning; bogus set rejected (exit 1). Active reconcile removes container.clusterAdmin/admin + monitoring.admin + logging.admin when not selected. **Cloud verified 2026-07-24 (scratch `adamparco-kage`):** live platform GSA had admin (pre-P1-T9 provisioning); ran the real `execute_platform_agent` reconcile → `gcloud get-iam-policy` now viewer-only (7 viewer roles; no clusterAdmin/admin/monitoring.admin/logging.admin); WI binding to `[kubeagents-system/kubeagents-platform-agent]` intact. Live create-attempt intentionally not run (would create billable cluster; IAM policy is authoritative — no create permission).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-07-24 | 1     | A1/invariant #2 — skills propose via GitOps only (T3)                    | local (grep/py)      | ✅ PASS   | No `create_cluster` in `agents/` (only the prohibition note); no `gcloud container clusters create/update` in the two rewritten cluster skills. `submit_suggestion.py` py-syntax OK; `resolve_tier` (flag>$AGENT_TIER>platform) validates the enum, `push_branch` refuses protected + non-`<tier>-agent/` branches. Other `gke-*` skills' `gcloud` prose is the deferred follow-on pass (phase-1.md §Deferred). **Verified 2026-07-24:** `submit_suggestion` guardrails exercised in isolation — `push_branch` refuses `main`/`production` and cross-tier namespaces, accepts valid `platform-agent/…` and issues the correct `git push`; live PR open exercised by the Phase 1 PR itself (A3 audit record).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-24 | 1     | A1/06 §4 — actuation pipeline is sole writer (T10)                       | local (shell)        | ✅ PASS   | `apply.yml` parses (ruby YAML); target-detection + apply_path dispatch unit-tested standalone (KCC→kubectl, HCL→terraform, empty/absent skip). Least-priv per target via one GitHub Environment per target (own WIF provider + deploy SA); on merge to `main` for `clusters/**`+`fleet/**`; prune off (deletions explicit). Live pipeline run pending a real cluster.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-07-24 | 1     | 05 §3 — controller + agent images pinned (T11)                           | local (go test)      | ✅ PASS   | Every `:latest` in the deployed surface replaced with `v0.1.0` (manager.yaml, kustomization.yaml, Makefile IMG, defaultPlatformAgentImage, both shipped CRs). Golden regenerated → `platform-agent:v0.1.0`. `go build/vet/test ./...` green. No published image yet, so digest-pinning documented for production (comments on every pin).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-07-24 | 1     | 08 §2 — Scion launch spike + native fallback (T12)                       | local (go test)      | ✅ PASS   | Pod-construction seam (`pod_launcher.go`): `selectPodLauncher` returns native by default; gate `KUBEAGENTS_SCION_LAUNCH` on + `scionK8sModeAvailable()` false ⇒ **falls back to native** (unit-tested); Scion-vs-native pod parity (identity/placement/sandbox/hardened securityContext). `reconcileDeployment` builds through the seam; golden unchanged (no behaviour change). `go build/vet/test ./...` green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-24 | 1     | 03 §11 — attenuation admission re-run (load-bearing)                     | Kind v1.31.2         | ✅ PASS   | Phase-0 backstop re-run under Phase 1: `negative-attenuation.sh` → write-verb Role DENIED, impersonate ClusterRole DENIED, wrong-scope ClusterRole DENIED, read-only Role ADMITTED (exit 0). Pre-created platform identity overlay (T6) applied VAP-clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-24 | 1     | **Phase 1 acceptance A1–A4** (07)                                        | Kind + scratch       | ✅ PASS   | **A1** structural: no `create_cluster`/`mcp-gke` in rendered config; skills author KCC + `submit-suggestion`; `apply.yml` sole writer. **A2** K8s: `auth can-i` writes denied; cloud: GSA reconciled to viewer-only (scratch `adamparco-kage`). **A3** PR is the audit record (this PR). **A4** closed `allowedUsers` enforced by CEL; read-only-within-tier via explorer ClusterRole + VAP. `go build/vet/test ./...` green; codegen clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-24 | 2     | 06 §1/§1.1, 08 §2 — CRD rename + fold (P2-T1..T6)                        | local (go test)      | ✅ PASS   | `go build/vet/test ./...` green (`d50b27c`). Golden semantic `cmp.Diff` clean — only `kind`/ownerRef.kind `PlatformAgent`→`Agent`. New `agentindex` unit tests: per-tier `ScopeIdentity` key, `Identity`↔`ScopeIdentity` no-drift, distinct clusters don't collide. New webhook `TestAgentScopeAndParent`: non-platform tier missing `projectId`/`clusterName`/`parentRef` (and dev-team `namespace`) REJECTED, complete ADMITTED; existing cardinality + tier-immutability + closed-allowlist suites still green. `make generate`/`make manifests` clean; old `platformagents` CRD deleted, `agents` CRD present with folded harness/integration/tier/scope/parentRef. Whole-tree residual-`PlatformAgent` grep clean (only `defaultPlatformAgent*` + godoc + unrelated `credential_proxy.py` docstrings). **Live Kind webhook-serving gate = P2-T7 (pending verification stage).**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-07-24 | 2     | V-K1 — live post-rename webhook serving (P2-T7)                          | Kind v1.31.2         | ✅ PASS   | Fresh cluster; full stack deployed (cert-manager→`make deploy`→VAP). `/validate-…-agent` CA injected. Valid duplicate `(tier,scope)` (differ only in name) → `Duplicate value: … (tier, scope) must be unique`; `tier` PATCH → `tier is immutable` (tier unchanged). Malformed-object rejections adversarially distinguished from policy denials.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-07-24 | 2     | V-K2 — VAP attenuation (load-bearing, 03 §11)                            | Kind v1.31.2         | ✅ PASS   | `negative-attenuation.sh`: write-verb Role, impersonate ClusterRole, wrong-scope developer-team ClusterRole all **DENIED** (message match from policy); read-only Role **ADMITTED**. `failurePolicy=Fail`, K8s 1.31 (VAP GA).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-07-24 | 2     | V-K3 — read-only per-tier SAR / accept (b) (P2-T23)                      | Kind v1.31.2         | ✅ PASS   | `auth can-i --as=system:serviceaccount:kubeagents-system:cluster-admin-agent`: get/list/watch=yes (pods/deploys/nodes/configmaps/namespaces); create/update/patch/delete/deletecollection=no; impersonate/escalate/bind/create-clusterroles=no; `* *`=no. ClusterRole `cluster-admin-agent-explorer` grants only get/list/watch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-24 | 2     | V-K4..K7 — rendered no-write + router (P2-T11/18)                        | local (go test)      | ✅ PASS   | `go test ./...` green: `TestAgentsGolden`+`agent_manifests_test` (rendered pod bound to cluster-admin SA+image, closed allowlist, no write tools); router resolve both modes→`(cluster-admin,c)` `inference_calls==0` (c); non/empty-allowlist refused before dispatch `callCount==0` (d); Pub/Sub-emulator `PubSubDispatcher` happy path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-24 | 2     | V-K8 — cascade render → VAP dry-run / accept (a)                         | Kind v1.31.2         | ✅ PASS   | `propose-cluster-admin` render (all flags) → placeholder-free bundle (CR+identity+bootstrap+egress). Rendered identity **ADMITTED** by VAP `--server-side --dry-run=server`; write-verb tamper (`create`) **DENIED** with the read-verb policy message.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-24 | 2     | V-K9 — spoke bootstrap ordering (P2-T21)                                 | Kind v1.31.2         | ✅ PASS   | On the fresh cluster: out-of-order Agent CR before CRD → `no matches for kind "Agent"` (FAILS). In order (cert-manager→controller/CRD/webhook/router→VAP→identity→CR): controller reconciled `cluster-admin-cluster-a-gateway` bound to pre-created `cluster-admin-agent` SA + `ghcr.io/…/cluster-admin-agent:v0.1.0` + `kube-agents/tier=cluster-admin`. Pod Pending only on single-node Insufficient memory (spec correct). Router CrashLoop expected (placeholder `KAGE_PROJECT_ID`/no GCP creds — fails fast by design).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-24 | 2     | V-K10 — no break-glass (P2-T23)                                          | local + Kind         | ✅ PASS   | `config/rbac/role.yaml` has no write verb on rbac resources; router ClusterRole only `agents get/list/watch`; no RBAC create/update/patch/delete in controller/router Go.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-07-24 | 2     | V-K11 — cross-cluster egress enforcement (P2-T22)                        | Kind + **Calico**    | ✅ PASS   | kindnet does not enforce NetworkPolicy → throwaway `kube-agents-netpol` Kind cluster w/ Calico v3.27.3. Default-deny egress + DNS + `1.1.1.1/32` allowlist selecting `tier=cluster-admin`: allowed CIDR **REACHABLE**; `8.8.8.8`/`9.9.9.9`/**metadata `169.254.169.254`** **BLOCKED**; unlabeled pod unaffected (selector scoping). Deterministic half (on the main cluster): netpol valid, selects the reconciled pod by tier label, zero `0.0.0.0/0`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-24 | 2     | **Phase 2 consolidated Kind gate**                                       | Kind v1.31.2         | ✅ PASS   | `local-dev/kind/verify-phase2.sh` (new, re-runnable) — preconditions + V-K1/K2/K3/K8/K9/K10 all green in one run; V-K4–K7 via `go test`, V-K11 via Calico cluster (above).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-24 | 2     | V-G1..V-G4 — scratch GKE cloud validation (P2-T24)                       | scratch GKE          | ⏸ PENDING | `adamparco-kage` reachable but infra not provisioned: cluster-admin viewer GSA+WI (V-G1), 2nd spoke + private hub Inference/Minty (V-G2, cross-cluster private = Phase 3+; inference proxy = Phase 5), live Google Chat app (V-G3), live CI/CD pipeline (V-G4). Per directive: code + full Kind verification landed; V-G flagged pending scratch-GKE, **not** faked. All load-bearing security negatives proven on Kind+Calico.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-07-24 | 3     | P3-T7 — `propose-developer-team` render golden parity                    | local (python)       | ✅ PASS   | `render_developer_team.py --cluster cluster-a --namespace team-x --project-id your-gcp-project-id --location us-central1` into a tmp repo → all seven manifests (00–60) **byte-identical** to the committed `clusters/cluster-a/namespaces/team-x/` exemplar (`diff -q` clean, golden rc=0). Emits no `bootstrap/` or VAP waves. `submit_suggestion.py` `VALID_TIERS` includes `developer-team` (tier accepted; branch guard scopes to `<tier>-agent/`). py_compile + prettier clean. Live VAP/webhook dry-run + Calico egress deferred to T13 (Kind).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-24 | 3     | 06 §2b/§10 — router NL mode + dev-team routing (T8–T10)                  | local (go test)      | ✅ PASS   | `go build/vet/test ./...` green; gofmt clean. **T8:** `TestIndex_LookupHandle` + `TestGateway_DeveloperTeamRouting` — `@devteam-<ns>` single-cluster routes (InferenceCalls==0), undeployed → `ErrNoSuchTarget`, multi-cluster → `ErrClarify` (2 candidates via `errors.As`), scope-edit evicts stale `byTierLeaf` slot; old deferred sentinel grep-clean. **T9:** `TestResolve_InferenceBoundary` + `TestInfer_RouteClarifyAndAccounting` + `TestInfer_ThresholdFlipByConfigOnly` — `Resolve` deterministic-only → `ErrNeedsInference` (0 spend); `Infer` sole increment site (empty/nil-inferer 0 spend, route AND clarify 1 spend); route↔clarify flips only via `WithThreshold`. **T10:** `TestGateway_InferenceCandidateValidity` + `TestInferredHandleStaleMenuRefusedBySpine` — hallucination filtered → `ErrUnaddressed` 0 sends; mis-inference to real agent w/o allowlist → `ErrUnauthorized` 0 sends; stale-menu handle refused by live spine. Live Kind router-serving deferred to T13.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-07-24 | 3     | 06 §6/§2b — thread affinity + audit surface (T11–T12)                    | local (go test)      | ✅ PASS   | `go build/vet/test ./...` green; gofmt clean. **T11 (`b440a9a`):** `TestMemAffinityStore` (injectable clock: empty-input no-ops, bind/lookup/drop, TTL lapse at exactly +30m, refresh extends) + `TestGateway_ThreadAffinity` (6 subtests) — deterministic turn binds; bare follow-up sticks `ModeSticky` with `spy.calls==0`; explicit @handle rebinds; bound thread still authorizes (mallory `ErrUnauthorized`, binding undamaged); refused turn writes no binding; TTL expiry + stale-agent binding dropped → fall-through. **T12 (`70694f4`):** `TestGateway_AuditAttributionSurface` (6 subtests) — delivered turn records Tier/Identity/ThreadID; sticky follow-up records `Mode==ModeSticky`+Tier+Identity; NL clarify records `Clarify==true`,`Mode==ModeInference`,`Dispatched==false` and fake `Replier` got the candidate menu (both handles named); ambiguous @handle clarify records `Clarify==true` with the handle's tier; nil Replier still audits; Replier failure folds into the audit reason. Live Kind router-serving deferred to T13.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 2026-07-24 | 3     | **Phase 3 consolidated Kind gate** (07 §2 Accept + 03 §3/§11)            | Kind v1.31.2         | ✅ PASS   | `local-dev/kind/verify-phase3.sh` (`3a08bdb`) — 30 checks in one run: P3-K1 placement (metadata.namespace==scope.namespace ADMIT / foreign metadata.namespace REJECTED — A1 isolation-escape blocked), P3-K2 reconciled pod (SA `developer-team-agent` + `developer-team-agent:v0.1.0` image + `kube-agents/tier=developer-team`), P3-K3 SAR (get/list/watch in team-x only; kube-system/nodes/writes/impersonate/escalate/bind/`* *` all **no**), P3-K4 duplicate (tier,scope) + tier PATCH REJECTED, P3-K5 attenuation (delegate to `negative-attenuation.sh`), P3-K6 netpol shape + ExternalName aliases (default-deny + egress tier selector, **no 0.0.0.0/0**, server-dry-run valid; Calico enforcement deferred/flagged), P3-K7 cascade render→VAP (identity ADMIT / write-verb tamper DENY). Exit 0. **Stale-image caveat:** the deployed operator predated the placement clause and had to be rebuilt+reloaded before this run enforced A1 (see Decisions & deviations).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-24 | 3     | Regression — verify-phase2 + `go test ./...` (03 §11 not regressed)      | Kind v1.31.2 + local | ✅ PASS   | Post-image-refresh: `verify-phase2.sh` exit 0 (V-K1/K2/K3/K8/K9/K10 green — 03 §11 negatives via `negative-attenuation.sh` DENY write/impersonate/wrong-scope, read-only ADMIT); `go test ./...` all packages ok; `go vet` + `gofmt -l` clean on router/webhook/controller. No Phase-0/1/2 acceptance regressed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-24 | 4     | P4-T1..T4 unit/build (Track S + Track A)                                 | local (go/py)        | ✅ PASS   | `go build ./...` + `go test ./...` green (FAIL=0), gofmt/vet/prettier clean. **T3** (`643af03`): `TestWatcherArgsPerTierScoping` (platform/cluster-admin → `--owner=<tier>`, no scope-ns; dev-team → `--owner=developer-team` + `--scope-namespace=team-a`) + `TestValidateScopeNamespace` (dev-team w/o scope-ns → fail-closed error) + cluster-admin golden 1-line diff. **T4** (`5e75146`): `eventingress` relay/normalize/pubsubsource — `TestRelayDeliverSendsAuthAndEnvelope` (Bearer + X-Asserted-Caller on both calls, kind in inner msg), `TestSource_DrainsAlertsToRelay` (pstest emulator: alert drained→{kind:alert}, poison Acked+dropped), `TestRunSynthetic` (Kind terminus e2e through the real relay). Cloud transport scratch-GKE-deferred (subscribe-only, emulator-proven). T1/T2 seam auth + kind-branch carried from `273848a`/`b4fe003`. Kind acceptance a–e deferred to P4-T11 gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-24 | 4     | P4-T5..T8 unit (Track B + Track C hermetic)                              | local (py)           | ✅ PASS   | `python3 -m unittest` over `local-dev/` → **21 tests OK**; prettier clean. **T8** (`b08ebe5`): `test_no_false_positive_on_server_defaults` (LIVE_CLEAN = authored fields + uid/resourceVersion/creationTimestamp/generation/managedFields/last-applied-annotation/status → **rc 0**), `test_detects_real_field_change` (drop `Egress` from live `policyTypes` → **rc 2**, "DRIFT DETECTED"), `test_emit_corrective_artifact_without_push` (rc 2; artifact branch `platform-agent/drift-networkpolicy-default-deny`; diff contains `knowledge/observation/drift-…​.md` + `type: observation` + `policyTypes`; **live JSON byte-identical before/after** — read-only; `_NoNetwork` proves no refresh/push/PR). Kind acceptance (e) via the dry-run artifact deferred to the P4-T11 gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-07-24 | 4     | P4-T9 structural (per-tier heartbeat SOPs)                               | local (py)           | ✅ PASS   | `python3 -m unittest discover local-dev` → **28 tests OK** (7 new). `test_heartbeat_sops`: both SOPs carry the backstop properties (`backstop`, last-resort, `04 §4`, read-only, `NO_REPLY`, submit-suggestion, raise-escalation, `invariant 3`); cluster-admin SOP is cluster-scoped w/ other-cluster/fleet over-reach guard + escalates fleet-wide UP; developer-team SOP is namespace-scoped w/ "cannot read other namespace/cluster-scoped" guard + escalates cluster-scoped to parent; both `cron/jobs.json` wire an enabled `heartbeat` job (schedule `*/15`) reading `heartbeat_sop.md` w/ read-knowledge/submit-suggestion/raise-escalation; dev-team heartbeat prompt never advertises a cluster-wide sweep; no duplicate job ids. Kind acceptance (d) deferred to the P4-T11 gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-07-24 | 4     | P4-T10 — OKF seed (runbook + escalation scaffold)                        | local (py)           | ✅ PASS   | `okf-validate.py examples/gitops-repo/knowledge` → **PASSED: 3 entries, all typed, links resolve** (was 2); negative control on a frontmatter-less entry still exits 1. read-knowledge smoke: `--type runbook` lists `runbook/pod-crashloop.md`, `--link runbook/pod-crashloop.md` prints it, `--type escalation` → no entries yet (exit 4, expected; T6 proves post-raise retrieval). Runbook is read-only triage → correction via submit-suggestion / raise-escalation only. Kind acceptance (c) end-to-end deferred to the P4-T11 gate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-07-24 | 4     | P4-T11 — Phase-4 gate (verify-phase4.sh) + go-test + regress             | local + Kind         | ✅ PASS   | `verify-phase4.sh` **exit 0 — ALL CHECKS PASSED**. HERMETIC acceptance (a–e): **(a)** `TestValidateScopeNamespace` (ns-tier w/o `--scope-namespace` fail-closed REJECTED; cluster tiers OK) + `TestWatcherArgsPerTierScoping` (controller renders `--owner=<EffectiveTier>` + `--scope-namespace` only for dev-team) + seam `test_inject_auth` (S1) / `test_inject_render` (S2) across all 3 tiers; **(b)** `test_raise_escalation` + structural invariant-3 (dev-team & cluster-admin egress netpols carry NO parent-tier destination — cross-tier flow is GitOps+loopback only); **(c)** `test_read_knowledge` (sparse `knowledge/`-only, refuses push) + okf-validate PASS + frontmatter-less negative control REJECTED; **(d)** `test_heartbeat_sops` (cluster-admin=cluster / dev-team=namespace-only; NO_REPLY; change→PR; backstop); **(e)** `test_detect_drift` (drifted object still present) + `test_submit_suggestion` (`--dry-run` halts before push/PR). REGRESS (live Kind `kind-kube-agents-dev`): `negative-attenuation.sh` (03 §11) green; dev-team watch identity still read-only + ns-scoped under a trigger (get/watch=yes; create/x-ns/cluster=no — invariant 1); controller golden (08 §7 mints no RBAC). Also: `go test ./...` **10 pkgs ok**; **verify-phase3.sh not regressed** (P3-K1..K7 all PASS). DEFERRED not faked: live Event→session spawn + cloud transport (Pub/Sub/GitHub HMAC) → Phase-4 image rebuild / scratch-GKE; **05 §8 chaos → Phase 6 (N-A)**; Calico egress enforcement + V-G cloud identity per verify-phase3. (`8df6e00`)                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-07-24 | 5     | **Phase 5 consolidated gate** (verify-phase5.sh — Acc a–d) + regress     | Kind v1.31.2 + local | ✅ PASS   | `verify-phase5.sh kind-kube-agents-dev` **exit 0 — ALL CHECKS PASSED**. **(a)** `score_findings.py`: unmitigated `high` → **exit 1 BLOCK**, clean `[]` → exit 0 PASS, matching non-expired waiver → exit 0 (mitigated), **EXPIRED waiver → exit 1** (negative control, no silent bypass); scorer (33) + extractor (7) unit suites green. **(b)** all three tier egress netpols pure allowlist (`policyTypes:[Egress]` + tier `podSelector`, **no `0.0.0.0/0`**); LIVE `egress-enforcement.sh` DEFERRED on kindnet (rc 3, non-fatal) — **enforcement PROVEN on Calico** (P5-T6). **(c)** go `TestAgentsGolden`+`TestClusterAdminRender_LoadBearing` (readOnlyRootFilesystem on EVERY container) + PSS `enforce: restricted` label + both VAPs present; **LIVE VAP dry-run**: un-hardened `kube-agents/tier` pod **REJECTED** (error names `readOnlyRootFilesystem`, polled past binding-activation delay), hardened **ADMITTED**, non-agent pod **UNTOUCHED** (scope proof). **(d)** `test_submit_suggestion.py` (`Requested-by:`/`Trace-Id:` trailers — flag>env>autonomous, single-line, idempotent, reach artifact) + go `TestGateway_AuditAttributionSurface` (delivered turn ties `Sender`↔`TraceID` to dispatch). REGRESS: 03 §11 `negative-attenuation.sh` green; `verify-phase{2,3,4}.sh` all exit 0 (not regressed); `go test ./...` green. **05 §8 chaos → Phase 6 (N-A)**. (`c4a9abb`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-24 | 6     | **Phase 6 consolidated gate** (verify-phase6.sh — chaos C1–C4 + regress) | Kind v1.31.2 + local | ✅ PASS   | `verify-phase6.sh kind-kube-agents-dev` **exit 0 — ALL CHECKS PASSED**. **A. 05 §8 failure isolation (chaos, NET-NEW load-bearing)** via `chaos-suite.sh` (reversible, single-object, `kind-*`-guarded, self-cleaning `kube-agents-chaos` ns; poll continuity across the whole window, bounded recovery waits): **C1** controller→0 — a running stand-in pod stays Ready (running pods continue), the deleted **real** agent Deployment is NOT recreated (no reconciles while down), scale-up → controller re-acquires leadership and recreates it (reconcile/provisioning RESUMES) [Accept b]; **C2** controller up — deletes the real agent Deployment → recreated (owned by Agent CR `cluster-admin-cluster-a`), deletes a pod → Deployment recreates it [Accept c]; **C3** kill cluster-admin stand-in pod → developer-team pod UID-stable + Ready across the whole window (**NO cascade**, 04 §6) + cluster-admin relaunched [Accept b]; **C4** hub-inference stand-in→0 → spoke-workload stays Ready (last-applied state survives), structurally decoupled (own ReplicaSet, no hub ownerRef) [Accept a]; + 05 §8 bullet-4 unopinionated actuation (no Config Sync/Connector/Argo/Flux CRD). Fixtures (D1): reconcile-behaviour on the REAL Agent CR/controller; pod-continuity/no-cascade on hardened tier-labeled `pause` stand-ins (readOnlyRootFilesystem/runAsNonRoot/drop-ALL/seccomp, admitted under PSS-restricted + hardening VAP). **B. Full regression**: `verify-phase5.sh` → `verify-phase{2,3,4}.sh` + 03 §11 `negative-attenuation.sh` + goldens + `go test ./...` all green. First point the **live controller-rendered** agent pod is observed carrying `readOnlyRootFilesystem: true` and passing the hardening VAP end-to-end (see Decisions: stale-image recurrence). DEFERRED not faked (D3): literal spoke agent-reasoning-pause under real hub loss → two-cluster/scratch-GKE. (`local-dev/kind/verify-phase6.sh`) |
| 2026-07-24 | 7     | 06 §1.1/§4, 01 §6 — IaC parity KCC↔Terraform (P7-T1, Accept a)           | local (python)       | ✅ PASS   | `iac-parity.py` **exit 0** (14 facts). Committed matched pair: `clusters/cluster-a/provisioning/*.yaml` (KCC, `iac.format: kcc`) + `clusters/cluster-b/provisioning/*.tf` (Terraform HCL, `iac.format: terraform`). HCL **structurally valid** — required `terraform{}` provider pin + `resource "google_container_cluster"` + `resource "google_container_node_pool"` with required attrs, balanced braces; KCC↔HCL **semantically equivalent** — same location/release-channel/node machine-type+count/networking; `apply.yml` `apply_path()` dispatches `cluster-b/*.tf`→`terraform apply`, `cluster-a/*.yaml`→`kubectl apply`. Bad-HCL negative control → exit ≠ 0 (no silent pass). `terraform validate`/`fmt`/`apply` + real cloud apply DEFERRED-not-faked (D2; `terraform` binary absent — hermetic structural+semantic instead, same pattern as Calico for kindnet egress). `a92abc7`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-07-24 | 7     | 06 §4, invariant 2 — second CI/CD (CircleCI) dispatch parity (P7-T2, b)  | local (python)       | ✅ PASS   | `circleci-parity.py` **exit 0** (8 checks). `.circleci/config.yml` is valid YAML with `version: 2.1`, an apply job, and a workflow **filtered to `main`**; **dispatch parity** vs `apply.yml` — same KCC→`kubectl apply` / HCL→`terraform apply` routing + least-priv **per-target** creds (one context/env per target, own deploy identity); **no agent-held write credential introduced** (invariant 2 / D4 — the pipeline is the privileged writer, the agent still holds none). Malformed-config negative control → exit ≠ 0. `circleci config validate` DEFERRED-not-faked (D4; `circleci` CLI absent). `0ae06d7`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-07-24 | 7     | 01 §6 — provider-neutral observability seam (P7-T3, Accept c support)    | local (py + bash)    | ✅ PASS   | `observability-seam.py` (6 checks) + `otel-endpoint.sh` (6 checks) **exit 0**. OTLP endpoint moved from baked-at-build to `OTEL_EXPORTER_OTLP_ENDPOINT` (entrypoint-resolved): set → override honored; **unset → the exact `gke-managed-otel` default** (no regression); guarded rewrite (unset ⇒ no-op). Obs skill base URLs resolve from `KUBEAGENTS_OBS_BACKEND`/`OBS_*_BASE_URL`: default (unset) + explicit `gcp` → the exact `monitoring`/`cloudtrace`/`logging.googleapis.com` endpoints (unchanged); a per-signal override wins → non-`googleapis.com`; `prometheus` profile + explicit URLs → all three signals non-`googleapis.com`; **non-GCP profile with a required base URL unset FAILS LOUDLY** (`SystemExit`, no silent GCP fallback); all 5 obs scripts resolve via the `*_base_url()` seam with no hardcoded host. Live non-GCP backend queried end-to-end DEFERRED-not-faked (D3). `0bfc908`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-07-24 | 7     | **Phase 7 consolidated gate** (verify-phase7.sh — Accept a–c + regress)  | Kind v1.31.2 + local | ✅ PASS   | `verify-phase7.sh kind-kube-agents-dev` **exit 0 — ALL CHECKS PASSED**. **Section A** (T1–T3 hermetic): `iac-parity.py` + `circleci-parity.py` + `observability-seam.py` + `otel-endpoint.sh` all green. **Section B (P7-T4, Accept c — vanilla non-GKE target):** B0 the target is **vanilla upstream Kubernetes** — node `kubeletVersion=v1.31.2`, **no `-gke` suffix** (read from the authoritative SERVER node info, not the host's `-gke` client kubectl); B1 deterministic ChatOps routing `inference_calls==0` via `go test -run TestGateway_ThreadAffinity` (`--- PASS: …/deterministic_turn_binds`); B2 **no-GKE static assertion** — the cloud-neutral **mechanism** path (`vap-agent-readonly.yaml`, `vap-agent-pod-hardening.yaml`, `agent_webhook.go`, controller `role.yaml`, router `*.go` non-test) is clean of `googleapis.com`/`cnrm.cloud.google.com`/`container.google`/`.gke.io`/`gkehub`/`iam.gke` (the cloud-**coupled** WI→GSA identity annotation is `defer`-printed, D1); B3 live `verify-phase2.sh` + `verify-phase3.sh` green (read-only SAR, GitOps-PR-only, isolation, `(tier,scope)` webhook, VAP attenuation). **Section C (P7-T5 regression):** `verify-phase6.sh` **exit 0** → chaos C1–C4 + `verify-phase{2,3,4,5}.sh` + 03 §11 `negative-attenuation.sh` + goldens + `go test ./...` — nothing prior regressed (seam changes additive/default-preserving). **Section D:** deferrals printed (D1 second cloud, D2 terraform CLI, D2b circleci CLI, D3 live non-GCP obs, scratch-GKE V-G, 08 §5 hardening) — never asserted green. `0155e09`.                                                                                                                                                                                                                                                                                                                                                                |

---

## Definition of Done — 07 §3 (9 items; recorded at Phase 7, the final phase)

The roadmap's product-level Definition of Done spans **all** phases (0–7), not Phase 7 alone. Recorded
here at build completion with per-item evidence and any honest deferral. Every item is proven, or its
gap is a **deferred-not-faked** cloud/CLI dependency — never a silent pass.

| #   | 07 §3 Definition-of-Done item                                                                                                         | Status                      | Evidence / deferral                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | Platform operator provisions a cluster **only** through the Platform Agent (PR → CI/CD applies KCC/Terraform); zero manual `kubectl`. | ✅ MET                      | Phase 1 A1 (PR #3): no `create_cluster`/`gcloud container clusters create` in skills; skills author KCC + `submit-suggestion`; `apply.yml` is the sole writer (`apply_path()` KCC→kubectl / HCL→terraform), on merge-to-`main` only. Phase 7 P7-T1 adds the real Terraform target so "KCC **or** Terraform" is concrete. Live cloud apply = scratch-GKE V-G (deferred-not-faked).                                                                |
| 2   | Cluster admin provisions a namespace + Dev Team Agent **within guardrails**, human-approved.                                          | ✅ MET                      | Phase 2 F4 cascade (`propose-cluster-admin`) + Phase 3 `propose-developer-team` render a GitOps-PR bundle (golden rc=0); the `(tier,scope)` cardinality webhook + A1 placement clause + attenuation VAP are the guardrails; human approval = the reviewed PR (CODEOWNERS). verify-phase{2,3}.sh green.                                                                                                                                           |
| 3   | Dev team self-serves and is **provably unable** to affect another namespace or escalate.                                              | ✅ MET                      | Phase 3 P3-K3 read-only namespace-scoped SAR (get/list/watch in `team-x` only; kube-system/nodes/writes/impersonate/escalate/bind/`* *` all **no**) + P3-K1 placement clause (foreign `metadata.namespace` REJECTED — isolation escape blocked) + P3-K6 default-deny netpol (Calico-enforced, incl. metadata-server block). verify-phase3.sh 30 checks green.                                                                                    |
| 4   | All three agents **read-only**; the only write path is a reviewed PR.                                                                 | ✅ MET                      | 03 §11 `negative-attenuation.sh` (write-verb/impersonate/wrong-scope DENIED, read-only ADMITTED) — VAP `vap-agent-readonly` backstop; per-tier SAR read-only across all tiers; 08 §7 controller mints **no** RBAC/SA (golden + live). Re-run green under every phase gate incl. verify-phase7.sh.                                                                                                                                                |
| 5   | Agents coordinate **only indirectly** (GitOps + OKF); a negative test confirms no direct agent-to-agent call path.                    | ✅ MET                      | Phase 4 (PR #6): escalation round-trip is written via `submit-suggestion` and read back via `read-knowledge` (never a direct call); structural invariant-3 check — dev-team & cluster-admin egress netpols carry **no parent-tier destination** (cross-tier flow is GitOps + loopback only). Chaos C3 (Phase 6) also shows no cross-agent runtime coupling.                                                                                      |
| 6   | The review gate **blocks** an unmitigated high-severity change; every mutation is attributable + revertible.                          | ✅ MET                      | Phase 5 (PR #7): `score_findings.py` blocks an unmitigated `high`/`critical` (exit 1), mitigated only by a matching non-expired waiver (expired → still blocks, negative control); `Requested-by:`/`Trace-Id:` trailers on every mutation PR (attributable); squash-merge to `main` = revertible history. verify-phase5.sh Accept (a)+(d) green.                                                                                                 |
| 7   | Failure-isolation chaos tests (Phase 6) pass; **no cascade**.                                                                         | ✅ MET                      | Phase 6 (PR #8): `chaos-suite.sh` C1 (controller down — running pods continue, reconcile resumes on scale-up), C2 (controller relaunches deleted Deployment/pod), C3 (**no cascade** — dev-team pod UID-stable while cluster-admin killed), C4 (spoke survives hub loss, structurally decoupled). verify-phase6.sh exit 0; re-run green in verify-phase7.sh Section C.                                                                           |
| 8   | The human→agent boundary is secured by trusted-human access + a read-only ceiling.                                                    | 🟢 MET (one piece deferred) | Phase 1 closed `allowedUsers` (CEL-enforced) + Phase 5 end-to-end attribution (authenticated requester flows router→inject→session→PR) + the read-only ceiling (item 4). **Per-request user down-scoping** (attenuating the agent to the requesting human's own permissions) is explicitly **deferred hardening — 08 §5**, recorded not faked (alongside the cross-object webhook + gVisor sandbox).                                             |
| 9   | The Platform Agent **detects injected drift and opens a corrective PR unprompted** (SC4, 01 §7).                                      | ✅ MET                      | Phase 4 P4-T8 drift SOP + `detect_drift.py`: desired-authoritative diff detects a real field change (drop `Egress` from live `policyTypes` → rc 2 "DRIFT DETECTED"), emits a corrective-PR artifact (`knowledge/observation/drift-<slug>.md` + re-asserted manifest) while the drifted live object stays present (detect-and-propose, never fix); live JSON byte-identical before/after (read-only, no push). verify-phase4.sh Accept (e) green. |

**Roadmap status at Phase 7 completion:** Phases 0–7 all delivered (0/1/2/3/4/5/6 merged; 7 gate-green,
PR open + auto-merge pending). On the Phase 7 merge, the entire `docs/design/07-implementation-roadmap.md`
build is complete. Standing deferrals carried past completion (all recorded, none faked): scratch-GKE
**V-G** live-cloud checks (Phase 2), a **real second cloud** EKS/AKS identity + live apply (D1/D2),
**CLI-level** `terraform validate`/`circleci config validate` (binaries absent — hermetic parity instead),
a **live non-GCP observability backend** (D3), and the **08 §5** deferred hardening (cross-object webhook,
gVisor execution sandbox, per-request user down-scoping, L7 hostname egress proxy).

---

## Decisions & deviations

When a spec is genuinely silent and the harness picks an option, record it here (README rule #3).
This is what a reviewer reads to understand choices the specs didn't dictate.

| Date       | Phase | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Spec touched   |
| ---------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- |
| 2026-07-23 | —     | Harness verification fans out via subagents (Agent tool); the Workflow script is an optional accelerator requiring explicit opt-in.                                                                                                                                                                                                                                                                                                                                       | Agent tool needs no per-run opt-in, keeping autonomous runs unblocked.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | —              |
| 2026-07-23 | 0     | Scaffold the customer GitOps repo as a **reference layout inside this repo** at `examples/gitops-repo/` (the template customers fork); Kind/scratch bootstrap under `local-dev/`.                                                                                                                                                                                                                                                                                         | 06 §3's GitOps repo is external in prod and can't be created here; a versioned reference template is the buildable equivalent and satisfies "tree matches 06 §3".                                                                                                                                                                                                                                                                                                                                                                   | 06 §3, 07 P0   |
| 2026-07-23 | 0     | VAP read-only ceiling expressed as a read-verb **allow-list** (verbs ⊆ get/list/watch), not a write-verb deny-list.                                                                                                                                                                                                                                                                                                                                                       | Deny-list omitted RBAC escalation verbs; `impersonate` is standalone cluster-admin. Allow-list also closes any future non-read verb. Found by the pre-PR gate.                                                                                                                                                                                                                                                                                                                                                                      | 03 §4, §11     |
| 2026-07-23 | 0     | Per-tier RBAC "render overlay" delivered as static per-tier exemplars at `policy/rbac-overlay/`, not a kustomize/render script beside `agents/`.                                                                                                                                                                                                                                                                                                                          | A2 needs only that the pre-created overlay is the sole RBAC path; a tier+scope render isn't needed until a 2nd scope (Phase 1/2). Location/shape deviation from 06 §2 recorded.                                                                                                                                                                                                                                                                                                                                                     | 06 §2, §3      |
| 2026-07-23 | 0     | Binding / `aggregationRule` / non-tier-labeled RBAC checks deferred to the cross-object child⊆parent admission webhook.                                                                                                                                                                                                                                                                                                                                                   | v1 VAP is scoped to a role's own rules (07); the binding path is held by human review + CODEOWNERS meanwhile.                                                                                                                                                                                                                                                                                                                                                                                                                       | 07, 03 §11     |
| 2026-07-24 | 1     | Retire the `gke-admin` GSA permission preset entirely (not just re-default it). A stale value is coerced to `read-only` with a warning; provisioning actively removes admin bindings. `custom` remains for explicit, named-role opt-outs.                                                                                                                                                                                                                                 | Invariant #1 is explicit ("cloud GSA stays viewer-only"); shipping a one-word admin preset is shipping the write-capable-credential footgun the invariant forbids. Coercion (not hard error) lets existing `gke-admin` installs downgrade in place on re-provision.                                                                                                                                                                                                                                                                 | 07 P1, 03 §4   |
| 2026-07-24 | 2     | Per-cluster cluster-admin identity is a **flat materialized manifest** under `clusters/<c>/agents/identity/`, NOT a kustomize overlay (phase-2.md T12's file list assumed `kustomization.yaml`). It duplicates the tier exemplar `policy/rbac-overlay/cluster-admin.yaml` (byte-identical bodies) with a keep-in-sync header.                                                                                                                                             | The reference actuation pipeline (`apply.yml`) applies `clusters/<c>/agents/` with `kubectl apply --server-side --recursive -f`, never `-k`; a `kustomization.yaml` in that tree breaks recursive apply (`no matches for kind Kustomization`) and cross-dir kustomize file refs are load-restricted. Flat YAML is the only pipeline-correct shape.                                                                                                                                                                                  | 06 §3, 08 §4   |
| 2026-07-24 | 2     | cluster-admin viewer-only GSA named `kubeagents-cluster-admin-gsa` (project-level, following the platform `kubeagents-platform-gsa` convention; 28 chars, within GCP's 30-char limit). Flagged: for >1 cluster-admin per project it must become per-cluster (distinct GSA or IAM Condition).                                                                                                                                                                              | Design is silent on the exact cluster-admin GSA name; the platform convention is the least-surprising choice and fits the length limit. Phase 2 proves on ONE cluster where a single viewer GSA suffices; the multi-cluster case is a flagged human decision (phase-2.md §Notes).                                                                                                                                                                                                                                                   | 06 §2, 08 §7   |
| 2026-07-24 | 3     | Dev-team identity + Agent CR live under `clusters/<c>/namespaces/<ns>/` as `50-developer-team-identity.yaml`/`60-developer-team-agent.yaml` (numeric-prefixed alongside the isolation manifests), NOT under `clusters/<c>/agents/` as phase-3.md T5's file list assumed. `propose-developer-team` renders the same seven-file bundle.                                                                                                                                     | Both are namespace-scoped (a namespaced `Role`; the Agent CR is pinned to the team namespace by the A1 placement clause), so co-locating them with the namespace's other resources under `namespaces/<ns>/` matches 06 §3's "namespaces/<ns>/ holds Namespace, RBAC, NetworkPolicy, ResourceQuota" and makes the whole tenant apply as one numeric-ordered directory (Namespace→quota→netpol→aliases→identity→CR). `agents/` stays the home of the cluster-scoped cluster-admin identity + CR.                                      | 06 §3, 03 §4   |
| 2026-07-24 | 3     | `propose-developer-team` templates are tokenized from the committed `cluster-a/team-x` exemplar; the exemplar was regenerated from those templates so `render(cluster-a, team-x, your-gcp-project-id)` reproduces the seven manifests byte-for-byte (golden rc=0). Cascade ships NO `bootstrap/`/VAP waves.                                                                                                                                                               | Makes the exemplar the canonical cascade output (a free T13 golden) and removes the exemplar's two divergent placeholders (identity GSA `PROJECT_ID` vs CR `your-gcp-project-id`; namespace-specific `TEAM_X` chat token). The dev-team is provisioned into a cluster that already has the control plane + agent-read-only VAP (from the cluster-admin F4 bootstrap), so re-shipping them would be wrong.                                                                                                                           | 05 §5, 06 §3   |
| 2026-07-24 | 3     | Inner-loop rule: **rebuild + `kind load` + `rollout restart` the operator image after any webhook/controller change before running a Kind gate.** Same-tag (`v0.1.0`) images do not auto-refresh, and `imagePullPolicy: IfNotPresent` keeps the stale node image until the pod is recreated against a freshly loaded one.                                                                                                                                                 | Discovered in T13: the deployed `k8s-operator:v0.1.0` predated the A1 placement-clause commit (`0026ddc`), so the live webhook silently **ADMITTED** a foreign-namespace dev-team Agent (a namespace-isolation escape) even though source + unit tests were correct. A stale image reads as green while under-enforcing a load-bearing invariant — the most dangerous kind of false pass. Recorded so future phases refresh the image (or bump the tag) before trusting a Kind result.                                              | 07 §5, 03 §11  |
| 2026-07-24 | 4     | Drift correction is **desired-authoritative**: `detect_drift.py` only diffs fields the GitOps manifest declares, so live's server-added defaults/controller fields are structurally not-drift (on top of the explicit ignore-set). The corrective PR **re-asserts the desired manifest** and always writes a `knowledge/observation/drift-<slug>.md` audit entry; when the desired file is already correct on disk, the observation entry is the reviewable payload.      | 04 §5.1 / 01 §7 SC4 say "open a corrective PR," not how to canonicalize. A whole-object desired-vs-live diff drowns in benign server defaults → noisy false-positive PRs (the panel's named defect). Desired-authoritative is the narrowest diff that still catches every drift in a declared field, and the observation entry guarantees a reviewable, revertible artifact (invariant 5) even when the fix is "re-run the existing rollout."                                                                                       | 04 §5.1, 06 §5 |
| 2026-07-24 | 6     | The dev-Kind `k8s-operator` image must be the locally-built **hardened** controller before a Phase 6 gate — the published `v0.1.0` predates the Phase-5 pod hardening and renders agent pods **without** `readOnlyRootFilesystem`, which `vap-agent-pod-hardening` correctly rejects at admission (a chaos-recreated pod never appears). Rebuilt (`make docker-build IMG=…:dev-p6`) + `kind load` + `set image`, then let the controller re-render the agent Deployments. | Recurrence of the stale-image trap first recorded for Phase 3 (row above): Phase-5 hardening was proven via goldens + **synthetic** VAP pods, so the live controller was never rebuilt; a **grandfathered pre-VAP pod** (VAPs don't evict existing pods) masked the drift and kept `verify-phase2` V-K9 green. Phase 6's chaos forces a pod **recreation**, which surfaced it — and closes the gap: the live controller-rendered pod now carries `readOnlyRootFilesystem: true` end-to-end. Reinforces the inner-loop rebuild rule. | 07 §5, 05 §8   |
| 2026-07-24 | 7     | **D1** — "second target" is proven at two honestly-separated layers: the cloud-**neutral core** (read-only agent SA + SAR isolation, GitOps-PR-only mutation, namespace isolation, `(tier,scope)` webhook, attenuation VAP) is asserted **live on vanilla Kind** with an explicit no-GKE-dependency check; the cloud-**coupled identity** (WI→viewer-GSA; EKS IRSA / AKS AAD) is **deferred-not-faked**.                                                                  | Kind is a vanilla, non-GKE Kubernetes distribution, so the core concepts genuinely pass on a second target with no GKE API; a real EKS/AKS account + cluster is out of reach here. Mirrors the Phase-2 V-G scratch-GKE deferral and 01 §6 ("GKE/GCP is the first and only fully supported target today"; portability is "direction, not a committed milestone").                                                                                                                                                                    | 01 §6; 07 P7   |
| 2026-07-24 | 7     | **D2** — IaC parity is proven as a matched pair of committed exemplars (`cluster-a` KCC, `cluster-b` Terraform HCL): a hermetic validator asserts HCL **structural validity** + KCC↔HCL **semantic equivalence**; `terraform validate`/`fmt`/`apply` + a real cloud apply are **deferred-not-faked**.                                                                                                                                                                     | Both KCC and HCL artifacts are model-authored (the generator is `gke-cluster-creator/SKILL.md` prose, not code), so the verifiable seam is a tested exemplar pair, not a generator. The `terraform` binary is absent on the host — structural+semantic parity via `python3` is the honest hermetic stand-in (same pattern as Calico for kindnet egress); `SKILL.md` names the exemplars canonical so model HCL matches the tested shape.                                                                                            | 06 §1.1, §4    |
| 2026-07-24 | 7     | **D3** — provider-neutral observability is a **seam with GKE as the default**, never a rip-out: OTLP endpoint via `OTEL_EXPORTER_OTLP_ENDPOINT` (entrypoint-resolved, `gke-managed-otel` default) + obs skill base URLs via `KUBEAGENTS_OBS_BACKEND`/`OBS_*_BASE_URL` (`gcp` default). A live non-GCP backend queried end-to-end is deferred-not-faked.                                                                                                                   | Keeps every current GKE install byte-for-byte unchanged when the new knobs are unset (no regression) while making a non-GCP target possible; the non-GCP profile fails loudly on a missing base URL rather than silently falling back to Google. No non-GCP backend is running here to query end-to-end.                                                                                                                                                                                                                            | 01 §6          |
| 2026-07-24 | 7     | **D4** — a second reference pipeline (CircleCI) changes **no trust boundary**: the pipeline is the privileged writer exactly like `apply.yml`; the agent still holds **no** cluster/cloud write credential. Proven equivalent by a dispatch-parity check; `circleci config validate` deferred-not-faked (CLI absent).                                                                                                                                                     | Invariant 2 says all mutation flows through the customer's actuation pipeline with no agent-held write creds; adding a second pipeline that mirrors `apply.yml`'s KCC/HCL dispatch + per-target least-priv creds introduces no new write path from the agent. The `circleci` binary is absent — structural YAML validity + parity is the hermetic stand-in.                                                                                                                                                                         | 06 §4; inv. 2  |

---

## Outer loop: first full install on a live cloud (2026-07-24)

Phases 0–7 were verified on Kind plus targeted scratch-GKE checks. This is the record of the
first **end-to-end install onto a real GKE cluster** (`adamparco-kage` / `platform-agent-host`,
us-east4) with **every first-party image built from source**. Three defects only reproduce on a
clean cluster — each had been masked by an environment that predated the constraint it violates.

### Defects found and fixed

| #   | Defect                                                                                                                                                                                                                                                                        | Why it was invisible until now                                                                                                                                                                                                                        | Fix                                                                                                    |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 1   | **`system-metadata` PVC was namespace-scoped, not per-agent.** The data PVC was already `agent.Name + "-data"`, but the system claim was a fixed name with `ReadWriteOnce`, so the **second** agent in a namespace deadlocked in `ContainerCreating` on a multi-attach error. | Every prior test ran **one** agent per namespace. The designed topology puts the cluster-admin tier alongside the platform tier in `kubeagents-system`, so no two-tier install could ever have come up.                                               | `systemPVCName(agent)` → `<agent>-system-metadata`; goldens + unit tests updated.                      |
| 2   | **Bundled LiteLLM and inference-replay pods violate the namespace's own Pod Security Standard.** Phase 5 labelled `kubeagents-system` `enforce: restricted` but never gave those two a `securityContext`, so both are rejected at admission.                                  | PSA does not evict running pods. The existing install's namespace predated the label, grandfathering both. Recreating the namespace breaks **inference outright**, because the `litellm` Service fronts the replay proxy once that integration is on. | Added `runAsNonRoot`/`seccompProfile`/`allowPrivilegeEscalation: false`/`drop: ALL` to both manifests. |
| 3   | **`provision_03` ignored `OPERATOR_IMAGE`.** It called `make deploy` with no `IMG`, silently shipping the published `ghcr.io/gke-labs` controller even when the operator had been built from local source into the project's registry.                                        | Nothing failed — the wrong image simply ran, which is precisely the stale-image trap recorded for Phases 3 and 6, in a new place.                                                                                                                     | Step 3 now passes `IMG`/`ROUTER_IMG` through and repoints the router's kustomize image.                |

### Decisions

| Date       | Decision                                                                                                                                                                                                                                                                                                                                                                                                                     | Rationale                                                                                                                                                                                                                                                                                                                                                          | Spec touched |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------ |
| 2026-07-24 | The Minty key import uses **openssl + gcloud** instead of cloning and `go run`-ing the upstream `abcxyz/github-token-minter` CLI. `gcloud kms keys versions import` wraps the key client-side; the only gap is GitHub issuing PKCS#1 where KMS wants PKCS#8, which is one openssl call. Where gcloud's own wrapping is unavailable, the RSA-OAEP/AES-KWP blob can be built with openssl and passed via `--wrapped-key-file`. | Removes a Go toolchain requirement, a network clone, and third-party code handling the App private key. It is also strictly more portable: `--target-key-file` needs the `cryptography` library inside the gcloud SDK's Python, which is absent and uninstallable on some hosts — the openssl path has no such dependency.                                         | 06 §4        |
| 2026-07-24 | **kage-router is parked at 0 replicas unless Google Chat is wired.** `provision_03` scales it down and annotates why; it is only scaled up when `GOOGLE_CHAT_ENABLED=true` and `CHAT_SUB_NAME` is set.                                                                                                                                                                                                                       | `config/router` ships `replicas: 1` with `REPLACE_WITH_*` env, so on a Slack-only install the router crash-loops immediately on `InvalidArgument`. A permanently failing pod in the control-plane namespace trains operators to ignore red status. Parking is honest: the component has no work to do.                                                             | 06 §2b       |
| 2026-07-24 | **Minty requires the GitOps repo and the GitHub App to live in a GitHub Organization.** Documented as a hard prerequisite rather than worked around.                                                                                                                                                                                                                                                                         | `pkg/server/source/github.go` calls `InstallationForOrg` unconditionally, and `GET /orgs/{name}/installation` 404s for personal accounts — so a user-owned repo can never mint a token, and installing the App does not help. The alternative (forking Minty to call `InstallationForRepo`) means maintaining a forked broker for a constraint a free org removes. | 06 §3        |
| 2026-07-24 | Child agent tiers are provisioned by a new **step 12** (`provision_12_deploy_agent_tiers.sh`) from committed templates, pinning `spec.deployment.image` from `AGENT_IMAGE`'s registry.                                                                                                                                                                                                                                       | The cluster-admin and developer-team tiers previously had no provisioning path at all — they existed only as GitOps exemplars, so bringing up the full hierarchy was manual. Pinning the image matters because `defaultImageForTier` silently resolves a `ghcr.io` default when the CR omits it, which would mix published and source-built images in one install. | 06 §1.1, §2  |

### Verified live

Platform, cluster-admin, and developer-team agents all `Ready` and running **source-built images
only** (zero `ghcr.io/gke-labs` containers; running digests match the Artifact Registry digests).
Inference reaches Gemini end-to-end through the replay proxy. The Phase 5 admission policies were
exercised negatively on the live cluster: a write-verb tier role and an unhardened agent pod were
both **rejected** (the pod test made PSA-compliant first, so the VAP was provably the rejector).
Namespace isolation and the read-only ceiling were re-confirmed by SAR against the live SAs.
Minty brokers a real token: the platform agent opened
[gke-agentic/adamparco-infra#1](https://github.com/gke-agentic/adamparco-infra/pull/1) using a
short-lived installation token, holding no long-lived GitHub credential.

### Still manual (cannot be automated)

Creating the GitHub App and installing it on the repository require a browser session — GitHub
exposes no API for either, and the App's private key is shown exactly once at generation. The
org requirement above is a hard prerequisite, not a script's job. Everything downstream of
"App exists, key downloaded" is automated by `provision_10`.

---

## Blockers

Open items that halt autonomous progress. Clear a row when resolved (move detail to the PR).

| Date       | Phase | Blocker                                                                                                                                                                                                                                                                                                                                                                                                                                   | Needs                                           |
| ---------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| —          | —     | None open (autonomous progress not halted). Phase 2 Kind inner loop V-K0..V-K11 all green; no HALT condition (03 §11) triggered.                                                                                                                                                                                                                                                                                                          | —                                               |
| 2026-07-24 | 2     | **V-G1..V-G4 (scratch-GKE) deferred, not blocking.** Live cloud verification (cluster-admin viewer GSA + Workload Identity; second spoke with private-hub Inference/Minty; live Google Chat app; live CI/CD apply) needs infra not yet provisioned and depends on later phases (inference proxy = Phase 5). The load-bearing security negatives are all proven on Kind + Calico. Flagged pending in the Phase 2 PR — **not** faked green. | scratch-GKE hub+spoke, Chat app, CI/CD pipeline |
