{{ .Chart.Name }} {{ .Chart.Version }} (app {{ .Chart.AppVersion }}) installed in namespace {{ .Release.Namespace }}.
{{- if and .Values.litellm.enabled (ne .Release.Namespace "kubeagents-system") }}

WARNING: this release is not in the kubeagents-system namespace, but the
agent's baked default model endpoint is
http://litellm.kubeagents-system.svc.cluster.local/v1 — the agent will NOT
reach the LiteLLM gateway installed here ({{ .Release.Namespace }}). Install
into kubeagents-system, or override the agent's model endpoint.
{{- end }}

{{- if .Values.platformAgent.enabled }}

Before the agent pod can start, the Secret "{{ .Values.platformAgent.credentials.secretName }}"
must exist in {{ .Release.Namespace }} with an API_SERVER_KEY entry.
{{- /*
  The model-provider key is read by the LiteLLM gateway, never by the agent, and
  vertex_ai has no key at all — printing the old unconditional
  "ANTHROPIC_API_KEY, GEMINI_API_KEY, or OPENAI_API_KEY" told a vertex_ai
  installer to add a key that does not exist. Naming the one key that matters is
  also more useful than listing three.
*/}}
{{- if .Values.litellm.enabled }}
{{- if eq .Values.litellm.modelProvider "vertex_ai" }}
The LiteLLM gateway needs no model-provider key: vertex_ai authenticates to
Vertex AI with Workload Identity, through the "{{ .Values.litellm.vertex.ksaName }}"
service account this release created. The GSA it names
({{ .Values.litellm.vertex.serviceAccount }}) must already exist and hold
roles/aiplatform.user on {{ .Values.litellm.vertex.project }} — see the chart README.
{{- else }}
The LiteLLM gateway reads its model-provider key from the same Secret:
{{ upper .Values.litellm.modelProvider }}_API_KEY, for litellm.modelProvider={{ .Values.litellm.modelProvider }}.
{{- end }}
{{- else }}
litellm.enabled=false, so no gateway is installed here; the model-provider key
belongs wherever the gateway you run reads it from.
{{- end }}
{{- if .Values.platformAgent.credentials.create }}
This release created it from platformAgent.credentials.data, generating
SESSION_KV_API_KEY and SESSION_KV_SALT (preserved across upgrades).
{{- else }}
Two further keys are read from the same Secret: SESSION_KV_API_KEY and
SESSION_KV_SALT. Without them the agent still starts, but the in-pod
k8s-event-watcher exits on every start and NO cluster events are watched, the
pod-local Session KV server rejects every request, and identity pseudonyms are
not stable across restarts. See the chart README.
{{- end }}

For GCP access, bind your Google Service Account to the agent's KSA
"{{ .Values.platformAgent.security.serviceAccountName }}" via Workload Identity
(see the chart README).

NOTE — uninstalling: the PlatformAgent custom resource carries a finalizer that
only the operator can clear. Delete the PlatformAgent and wait for it to go
away BEFORE `helm uninstall {{ .Release.Name }}`, otherwise the CR strands:

  kubectl delete platformagent {{ .Values.platformAgent.name }} -n {{ .Release.Namespace }} --wait
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}
{{- end }}

Admission webhooks are not part of chart installs (CRD-level CEL validation and
OpenAPI defaulting still apply; the webhooks' extra validation, defaulting, and
delete-protection do not); the provisioning scripts / kustomize path provides
them.
