# clusters/cluster-a/agents/identity/broker-operations.yaml
#
# The APPLIED broker-operations grant for cluster-a — 06 §2.2.1. Materialized instance of the tier
# exemplar at ../../../../policy/rbac-overlay/broker-operations.yaml; everything below the header is
# byte-identical to it, and `dev/tests/actor-grant-single-sourced.py` checks every copy against
# 06 §2.2.1 itself rather than against each other (V-BRK-013, L0).
#
# ONE per cluster, shared by every actor identity in it: the objects are tier-neutral by
# specification, so a second copy would be a second thing to review with nothing to gain. The
# per-tier ClusterRoleBindings and RoleBindings that point at them live beside their own
# ServiceAccounts, in ./cluster-admin-identity.yaml and in each team namespace's
# 50-developer-team-identity.yaml.
#
# APPLY ORDER: `clusters/<c>/agents/` is applied recursively with `kubectl apply --server-side
# --recursive -f`, so file order within the directory is not guaranteed — and does not need to be. A
# RoleBinding whose roleRef does not yet exist is a legal object that simply grants nothing until the
# Role appears; the failure mode of the wrong order is a broker that cannot write its journal yet,
# not a broker that can write something it should not. The wave that matters is the VAP
# (clusters/<c>/bootstrap/20-policy/), which is enforcing before any of this is applied.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kubeagents-broker-operations
  labels:
    # No `kube-agents/tier`: the grant is tier-neutral by specification, and stamping a tier on a
    # shared object would be a lie the VAP would then enforce. `kube-agents/role: actor` is what
    # selects it into the policy's actor arm — which is why the policy's match condition tests for
    # either label rather than for tier alone.
    kube-agents/role: actor
    app.kubernetes.io/managed-by: gitops
rules:
  # step 1: authenticate the calling agent. Creates nothing; TokenReview is a request object.
  - apiGroups: ["authentication.k8s.io"]
    resources: ["tokenreviews"]
    verbs: ["create"]
  # step 5: the brake — MUST be readable by every tier. A tier that cannot read the freeze object
  # fails closed permanently (06 §4.4), so omitting this does not fail safe, it bricks the tier.
  - apiGroups: ["kubeagents.x-k8s.io"]
    resources: ["fleetfreezes"]
    verbs: ["get", "list", "watch"]
  # step 4: classification input.
  - apiGroups: ["kubeagents.x-k8s.io"]
    resources: ["changepolicies"]
    verbs: ["get", "list", "watch"]
  # steps 3 and 5: the Agent set. Read-only, and CLUSTER-SCOPED deliberately.
  #
  # 06 §2.2 appends this grant "verbatim to every actor Role/ClusterRole", and for the platform
  # and cluster-admin tiers that object is a ClusterRole -- so the spec grants this read at
  # cluster scope. It used to sit in the namespaced half below, on the reading that agents is a
  # namespaced kind. That is true of the kind and false of the requirement: `LowerTierOwner`
  # answers "which Agent's SCOPE covers namespace X", which cannot be found by listing in X,
  # so it lists cluster-wide on every namespaced operation and `resolveOwner` guards it with
  # nothing but `live != nil`. Granted namespaced, the ownership gate failed closed for every
  # envelope on every cluster, and nothing below L2 could see it -- `broker-execute-l2.sh` was
  # the first thing to submit a real envelope at a deployed broker and it stopped here.
  #
  # Moving it does not widen the grant: the (apiGroup, resource, verb) set is unchanged, which
  # is why V-BRK-013 stays green across this edit. Nor does it touch the split's actual rule --
  # "every verb that changes PERSISTED state lands in the namespaced half" -- because get, list
  # and watch change nothing. The two verbs that do write, `create actionrecords` and the status
  # update, stay below, bound by a RoleBinding, for the reason stated there.
  - apiGroups: ["kubeagents.x-k8s.io"]
    resources: ["agents"]
    verbs: ["get", "list", "watch"]
---
# The namespaced half. One instance per namespace that hosts an agent — this one is the
# `kubeagents-system` instance, which serves the platform and cluster-admin tiers; the team-x
# instance ships in that namespace's own bundle (clusters/cluster-a/namespaces/team-x/).
#
# A Role rather than a ClusterRole because these four resources are namespaced and because the two
# state-changing verbs in the whole grant live here: an actor can append to the journal in ITS OWN
# namespace and nowhere else. A ClusterRole would make the journal fleet-wide-writable, which is the
# fleet-wide-writer shape V-BRK-012 exists to forbid one layer down.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: kubeagents-broker-operations
  namespace: kubeagents-system
  labels:
    kube-agents/role: actor
    app.kubernetes.io/managed-by: gitops
rules:
  # step 11: journal — the broker owns its own records. `create` but never `update`/`delete`.
  - apiGroups: ["kubeagents.x-k8s.io"]
    resources: ["actionrecords"]
    verbs: ["get", "list", "watch", "create"]
  - apiGroups: ["kubeagents.x-k8s.io"]
    resources: ["actionrecords/status"]
    verbs: ["get", "update", "patch"]
  # step 7: approval input.
  - apiGroups: ["kubeagents.x-k8s.io"]
    resources: ["approvalrosters"]
    verbs: ["get", "list", "watch"]
