# The L0 regression chain — every check that runs with no cluster and no network.
#
# THIS FILE IS THE SINGLE DEFINITION SITE (V-MET-013). Three consumers read it and there is no
# second list to drift from:
#
#   1. .github/workflows/l0-checks.yml   — runs every line on every PR. Until 2026-07-25 CI ran
#                                          NONE of these, so every mechanization the harness built
#                                          across phases 2-8 was enforced only when a human
#                                          remembered to run it. That is LSN-007 ("built, tested,
#                                          and unreachable") pointed at the verification machinery
#                                          itself, and it is the reason this file exists.
#   2. dev/tests/invariants-gate.py — asserts every LESSONS.md entry marked `closed` names an
#                                          artifact that appears HERE. A lesson closed against a
#                                          command nobody runs is not closed (LSN-019).
#   3. the harness REGRESS step           — runs the chain before any CHECKPOINT.
#
# Format: one shell command per line, run from the repository root, expected to exit 0.
# Blank lines and #-comments ignored. Keep one command per line: the gate matches artifacts by
# substring, and a compound line hides what it runs.

# --- unittest modules (dev/test_*.py) -------------------------------------------------
# Discovered as one command; 148 tests at time of writing. Individually re-runnable with
# `python3 -m unittest dev.test_<name>`. Discovery is the wiring: a new dev/test_*.py needs no
# line here, which is why the count is written down -- it is the only thing that notices a module
# that stopped being discovered. Includes V-CMP-006 (dev/test_mcp_env_declared.py): an MCP server
# whose script reads a credential the server's own `env:` block never declares. Hermes passes a
# server only what it declares, so `agent_common` shipped credential-less and refused every
# inter-agent call from a pod that was Ready with green tests -- invisible to every other line here.
python3 -m unittest discover dev

# --- standalone L0 checks (dev/tests/*.py) --------------------------------------------
python3 dev/tests/install-path-wired.py
python3 dev/tests/closed-allowlist.py
python3 dev/tests/reference-render.py
python3 dev/tests/circleci-parity.py
python3 dev/tests/iac-parity.py
python3 dev/tests/observability-seam.py
python3 dev/okf-validate.py
# Callers vs parsers. P8-T4 removed `--github-cidrs` from the developer-team renderer and left
# verify-phase3.sh passing it: argparse exited 2, stderr went to /dev/null, and P3-K7 verified
# nothing for a day (LSN-021). Nothing in L0 looked at a CLI's callers until this line existed.
python3 dev/tests/cli-contract.py
# Quota sizing vs the pod it must admit. The agent gateway's footprint is decided in
# agent_manifests.go (3700m/6528Mi across four containers); the quota that has to hold it is
# decided in common.sh. On 2026-07-27 a hand-sized control-namespace quota of 8 CPU / 16Gi applied
# cleanly, grandfathered every running pod, and locked platform-agent-gateway out at 0/1 with
# `exceeded quota` — the install looked green the whole time. Nothing compared the two numbers.
python3 dev/tests/quota-admits-agent.py
# 09 §14's own lints (V-MET-010/011/012/013-for-check-IDs). They were written `L0` and never built,
# so 09 §5.1 lost two load-bearing components and 09 §5.2 five contracts with nothing to notice
# (LSN-014). V-MET-011 joined them in P8-T10 and polices `verification/traceability.yaml`, the
# curated map from all 176 Verification-section bullets in 01-08 to the checks that assert them —
# in BOTH directions, so a spec growing a bullet nobody verified is a red line here and not a
# catalog that merely looks complete.
python3 dev/tests/spec-ids.py
# Documentation a reader ACTS on, checked against the build (P8-T7). Three of the 74 falsehoods it
# first found were created by earlier units of phase 8 itself: P8-T1 deleted the allow-all escape
# hatch and left two documents still promising it, and P8-T3 shipped provisioning step 13 with no
# `make` target while every step list stopped at 11. Docs drift on the same schedule as code.
python3 dev/tests/docs-truth.py
# V-MET-005's YAML half: does the risk-classification corpus cover the rule set it claims to?
# corpus_test.go asserts the same property with a Go toolchain and a compiled package; this half
# needs neither and fails in seconds. Both halves matter. The one that earns its place is the
# NEGATIVE coverage requirement -- every gating rule needs a case asserting it stays QUIET, because
# positive-only coverage is satisfied by a classifier that gates everything, and a broker that gates
# everything is not a safe failure: it trains operators to approve without reading, which is how a
# gate stops being a control.
python3 dev/tests/classifier-corpus-lint.py
# V-REV-004's coverage half: does the undo round-trip corpus have a case for every verb in the 06
# §4.3.1 table, and does its negative set cover the three effects 09 §7.3 names by hand? The Go
# table test runs the cases and cannot notice one that was never written -- a corpus that quietly
# loses the `scale` row still passes every test in it. The check that earns its place is the
# POSITIVE one: a generator that refuses everything satisfies "no plan means gated" perfectly and is
# useless, so every verb needs a fixture where its inverse is seen to work.
python3 dev/tests/undo-corpus-lint.py
# V-GAT-017's L0 half: a closed import allowlist over internal/broker/classify, plus a ban on prose
# fields on its input surface. The L1 half (classify_test.go) permutes inputs and asserts the answer
# is byte-identical, which is the property that matters and is also limited to the inputs somebody
# thought to permute. This half is about shape, and the failure it prevents is not a deliberate one:
# it is the plausible refactor -- "let the classifier summarise the diff for the approval prompt" --
# that reaches for a chat client and turns the gate into a model's opinion. `time` is on the
# forbidden list beside the inference clients, for the same reason and with better odds of arriving.
python3 dev/tests/classifier-is-model-free.py
# LSN-032. The operator's API group is declared once, in groupversion_info.go, and this asserts that
# nothing restates it wrongly. Five of the forbidden set's nine entries named `kubeagents.gke-labs.dev`
# -- a group this operator has never served -- so `delete ActionRecord`, `patch ChangePolicy`,
# `delete FleetFreeze`, `create ApprovalRoster` and `patch Agent` fell through the forbidden check and
# classified `routine`. The corpus agreed, because its fixtures were written from the same wrong
# string. A wrong group is not a compile error and not a runtime error: the rule looks right, the
# fixture looks right, they match each other, and only the API server disagrees -- silently, by never
# matching. Found by reading; this is so the next one is not.
python3 dev/tests/api-group-single-sourced.py
# V-RUN-012's L0 half: `pause` is structurally not a scale-to-zero. 08 §2.4 and 06 §4.4 both promise
# the pod keeps running while an agent is paused -- same UID, same start time, same in-memory queue --
# so only the WRITE path closes, and it closes in the broker. The forbidden change is not exotic:
# `resolveDeploymentReplicasAndStrategy` already renders `replicas: 0` for the unrelated
# `spec.deployment.scaleToZero` idling feature, so wiring pause into it is one `||` three lines away
# from code that already does exactly that, it reads in a diff as tidy reuse, and it passes every test
# that does not specifically render a paused agent. The L1 half renders both and compares; this half
# asserts the shape that survives the renderer moving. `--negative-control` is the `¬` of 09 §6.
python3 dev/tests/pause-is-not-scale-to-zero.py
python3 dev/tests/pause-is-not-scale-to-zero.py --negative-control

# V-RUN-011's L0 half: the five 08 §2.5 label keys have one definition site and the scope value has
# one renderer. The L1 half proves `RenderScope` is injective over an adversarial corpus; it cannot
# prove the renderer is the ONLY renderer, and the second one is always three cheap lines at the call
# site (`"kube-agents/scope": agent.Spec.Scope.Namespace`) that neither truncate, sanitize nor hash.
# 03 §4.2 pins a pod to its ServiceAccount by comparing these labels, so a value built any other way
# is a selector that silently stops distinguishing two credentials. `--negative-control` is the `¬`.
python3 dev/tests/scope-label-single-sourced.py
python3 dev/tests/scope-label-single-sourced.py --negative-control

# V-CTR-003 and V-CMP-011, which are 06 §10's one sentence read from the contract side and the
# completeness side: the `Agent` CRD schema declares none of `rbac`, `rules`, `riskClass`,
# `scopeOverride`, `brokerServiceAccountName` or `actorServiceAccountName`, and sets no
# `x-kubernetes-preserve-unknown-fields` under `spec`. 03 §4.2 mints authority from (tier, scope) and
# never from the CR, and each of those six is the obvious field to add the first time someone needs an
# exception. Two arms carry the weight. Depth: `spec.rbac` is the spelling review catches and
# `spec.security.rbac` is the one that merges. And the GO SOURCE, because config/crd/bases is
# generated and no PR check regenerates it -- between adding the field and running `make manifests`
# the schema on disk keeps passing. The pruning itself is behaviour, proven at L2 by
# webhook-negatives-l2.sh's V-9 arm, which this check asserts still exists.
# `--negative-control` is the `¬` of 09 §6; two of its six mutations differ only in depth.
python3 dev/tests/crd-has-no-authority-fields.py
python3 dev/tests/crd-has-no-authority-fields.py --negative-control

# --- the broker pair (P9-T7b) ---------------------------------------------------------------
# V-BRK-012's source half: one broker per Agent CR, and no way to render half a pair. 08 §2.4 makes
# the broker per-agent because the actor credential IS the boundary -- a broker serving two agents
# holds both their write authorities and the isolation argument collapses to "the process is
# careful". Four properties: every broker-derived name is a function of the CR (a name that is a
# constant is a fleet-wide broker with extra steps); nothing outside the definition site spells the
# `-broker`/`-broker-tls`/`-mesh-tls` suffixes; the Deployment is single-replica and selects on both
# labels; and the pair-atomicity scaffolding is still load-bearing -- the last is the LSN-035
# non-vacuity arm, since a check for "no BuildDeployment" passes forever once the symbol is gone for
# an unrelated reason. `--negative-control` is the `¬`.
python3 dev/tests/one-broker-per-agent.py
python3 dev/tests/one-broker-per-agent.py --negative-control

# V-RUN-010 (P9-T9b-3): the broker's supply chain, image, socket count and mount set are minimal --
# 08 §2.1's "smallest possible supply chain" and 08 §2.6's shell-free, read-only, two-mount pod. Six
# properties, and the load-bearing one is the FIRST-PARTY IMPORT GRAPH walked textually from
# `cmd/broker`: no package this repository owns and the broker links may import a shell, a plugin
# loader, an interpreter, an inference client or a debug HTTP surface. It is textual because L0 CI
# has Python and no Go toolchain, and it is a graph walk rather than a scan of `cmd/broker` because
# all three defects it found on landing were a package or more away from main.
#
# Those three are why this check is not a formality. `internal/journal` held the Cloud Logging audit
# source, so the broker binary shipped `exec.CommandContext` on a configurable argv -- unreachable,
# since nothing constructed one, and one call site from reachable in the only process whose
# ServiceAccount can write. `cmd/broker` carried kubebuilder's scaffolded
# `_ "k8s.io/client-go/plugin/pkg/client/auth"`, which is a credential PLUGIN LOADER: its purpose is
# to let a kubeconfig name a binary the client will fork. And `ctrl "sigs.k8s.io/controller-runtime"`
# is a facade over `pkg/manager`, which imports `net/http/pprof`, whose init registers /debug/pprof
# on http.DefaultServeMux -- while main.go's own package doc had claimed "no pprof" since it was
# written. No image scan, SBOM or RBAC review shows any of the three: a distroless image with no
# /bin/sh is shell-free by every measure except the one that matters, which is what the process can
# still fork. The other five properties are the module SBOM (direct requires only -- `cel-go` is in
# the indirect closure by way of k8s.io/apiserver and no import discipline here removes it), the
# entry package's blank imports, the Dockerfile runtime stage (shell-free repo AND tag: `:debug` is
# the same base plus busybox), one listener agreeing with one containerPort and one servicePort, and
# the closed mount inventory on a read-only root. `--negative-control` is the `¬` of 09 §6; its base
# arm is there because the check's first draft matched the base by prefix and accepted `:debug`.
python3 dev/tests/broker-supply-chain-minimal.py
python3 dev/tests/broker-supply-chain-minimal.py --negative-control

# V-BRK-013 (P9-T7d-3): the broker-operations grant is written down once, in 06 §2.2.1, and every
# copy of it agrees with THAT rather than with a sibling copy. The grant currently exists in six
# places -- the spec, three copies of `vap-agent-readonly` (canonical, materialized, and the cascade
# template rendered into clusters that do not exist yet), the tier overlays, and the per-cluster
# identity bundles. Six copies of an allow-list is five chances for one to gain a verb, and the one
# that gains it is the one enforced in the cluster nobody is looking at. Nothing else would notice:
# RBAC is a union, so an extra rule silently grants; an allow-list entry nobody requests silently
# does nothing; and the only artifact that disagrees is a design document. Checked in BOTH directions
# -- 06 §2.2.1's point about `fleetfreezes` is that a DROPPED grant does not fail safe, it bricks the
# tier permanently (06 §4.4). Copies are discovered by glob (LSN-036) and the floors are FAILs rather
# than vacuous passes (LSN-035). `--negative-control` is the `¬` of 09 §6.
python3 dev/tests/actor-grant-single-sourced.py
python3 dev/tests/actor-grant-single-sourced.py --negative-control

# 06 §4.3 (P9-T7c-3c-ii-b-1): `vap-agent-scope-journal` enumerates ActionRecord's status fields one
# at a time, because CEL cannot index a struct by a runtime string -- and that enumeration FAILS
# OPEN. Validation 1 admits any write for which `nothingChanged` holds, `nothingChanged` is the
# conjunction over the ENUMERATED fields, so an UPDATE touching only an unenumerated status field is
# admitted from ANY principal, including the human cluster-admin the policy exists to stop. The file
# claimed in a comment that a catch-all named `unknownStatusFieldChanged` closed this; no such
# variable ever existed, and the claim survived several reviews looking like protection. It cannot
# be closed inside the policy: the expressible catch-all would also deny the no-op re-Update every
# controller does on a conflict retry. So parity is enforced here, before the policy ever loads --
# every json field of `ActionRecordStatus` has a `<field>Changed` variable, every such variable is
# in the conjunction, and each reads exactly its own field on every branch. Read from the GO TYPE,
# not the generated CRD, because the generated artifact is stale for the whole of the edit that adds
# a field. `--negative-control` is the `¬` of 09 §6.
python3 dev/tests/journal-status-vap-parity.py
python3 dev/tests/journal-status-vap-parity.py --negative-control

# V-CMP-007 (P9-T7d-5): every identity the broker resolves by name is created by something the
# install path RUNS. `install-path-wired.py` two lines up walks the script graph — every numbered
# step is invoked by its driver — and it was green for eight phases on a repository where the
# platform reader ServiceAccount was created by nothing (it exists on the live cluster because a
# human typed `kubectl create sa` once, with no labels), the actor identity and its 06 §2.2.1 grant
# lived only under examples/gitops-repo/ where no install path reads, and every reader that WAS
# created carried `kube-agents/tier` alone while both admission policies select on
# `kube-agents/role`. A policy whose selector matches nothing reports the same green as a policy
# that passes, which is why nothing noticed. This walks the three links past reachability-of-steps:
# template rendered by a reachable step, RBAC subject and roleRef resolving to objects the install
# path creates, and one `apply_agent_identity` call site per tier in agents/. It also holds the Go
# and bash derivations of the actor SA name together — 06 §2.2.1 forbids the CR from naming its own
# actor, so a drift surfaces nowhere except as BrokerReady=false. Tiers are read off agents/ and
# emitters discovered by body, not listed (LSN-036); the floors are FAILs (LSN-035).
# `--negative-control` is the `¬` of 09 §6, and one of its eight mutations is LSN-039 itself.
python3 dev/tests/identity-has-install-path.py
python3 dev/tests/identity-has-install-path.py --negative-control

# --- the install overlay renders, and renders faithfully (P9-T7d-6) --------------------------
# Two different properties, and only one of them is L0. That it BUILDS is `make render`, now a
# prerequisite of `make build` and `make test`; it needs the kustomize binary, so it cannot live
# here. That what it builds is the install anyone wants is this, and it needs nothing but the files.
# The distinction is the whole lesson: `config/default` was unrenderable for a month — an ambiguous
# replacement selector, introduced by the mesh CA itself — and `make deploy` was broken that entire
# time with every check green, because nothing in the repo ever rendered it. Pinning the selector
# then exposed the worse half: `namePrefix`/`namespace` had been silently rewriting the CA into
# `kubeagents-kubeagents-mesh-ca` in the wrong namespace, which is not the name `meshCAIssuerName`
# hardcodes and not a namespace a ClusterIssuer reads its Secret from. Neither rewrite errors. Both
# leave every agent Certificate Pending forever. So this asserts the reference graph instead of the
# output: no transforming kustomization can REACH config/mesh-ca (over the whole graph, not the one
# edge that broke), the Go constant and the ClusterIssuer name agree, the CA stays in cert-manager,
# and every remote pull — GitOps wave 10, the propose-cluster-admin template — names config/install.
# Empty result sets are FAILs (LSN-035). `--negative-control` is the `¬`; one of its nine mutations
# is the original bug verbatim.
python3 dev/tests/install-render-is-faithful.py
python3 dev/tests/install-render-is-faithful.py --negative-control

# --- builds compile whole packages (LSN-037) -------------------------------------------------
# `go build cmd/broker/main.go` compiles one file and pretends the rest of the package is absent.
# It is indistinguishable from the correct spelling until a package gains its second file, and then
# it fails on the symbols in the file it dropped. Dockerfile.broker broke exactly that way when
# `waitforbroker.go` landed: `go build ./...`, `go vet ./...` and `go test ./...` were all green and
# six of seven CI checks passed, because the image build was the only build in the repo not
# compiling packages. Late, far from the edit, and it reads as an infrastructure fault. Build inputs
# are globbed rather than listed, so a new Dockerfile is covered unedited (LSN-036), and finding no
# `go build` at all is a FAIL, not a vacuous pass (LSN-035). `--negative-control` is the `¬`.
python3 dev/tests/go-build-targets-packages.py
python3 dev/tests/go-build-targets-packages.py --negative-control

# --- every overlay an install path applies is rendered by something CI runs (LSN-042) --------
# The general form of the `config/default` outage. `make render` closed it for ONE overlay; four
# more -- LiteLLM base, the ChatGPT overlay, inference-replay, the GitHub integration -- were applied
# by `deploy-*` targets and rendered by nothing, for exactly the reason the first one was: nobody was
# looking at the list. Both sides are derived, so a sixth integration is enforced the day its
# `apply` line lands (LSN-036). Also asserts `render` is still a prerequisite of `build` and `test`:
# extending the recipe while unhooking it from CI restores the blind spot in one word.
# `--negative-control` is the `¬` of 09 §6; one of its seven mutations is that one word.
python3 dev/tests/install-artifacts-are-rendered.py
python3 dev/tests/install-artifacts-are-rendered.py --negative-control

# --- cluster-facing checks are not silently wrong about the cluster (LSN-044, LSN-045) -------
# Two ways an L2 suite passes while asserting something other than what it claims. (1) `auth can-i
# patch actionrecords/status` parses the slash as an object NAME, so it asks whether the subject may
# patch an ActionRecord called `status`. Every ClusterRole and VAP in the tree spells the subresource
# that way, so transcribing it is the natural motion -- and on a `want_no` the form is vacuously
# green, since the `no` comes from a name nobody was granted rather than from the policy under test.
# (2) The journal-retention VAP denies DELETE of an ActionRecord to the namespace controller and,
# absent an audit sink, to everyone else too: a suite that writes a record and then deletes its
# namespace passes once and can never create that namespace again. The protected set is DERIVED from
# the policies' own DELETE matchConstraints and the CRDs' `spec.names.kind`, so a second retention
# policy is enforced the day it lands (LSN-036). `--negative-control` is the `¬` of 09 §6.
python3 dev/tests/cluster-check-hygiene.py
python3 dev/tests/cluster-check-hygiene.py --negative-control

# --- every negative control names the rule it exercises (LSN-035) ----------------------------
# The `¬` of every other `¬` above. A control that asserts only "the suite went red" cannot tell
# you WHICH property caught the mutation, and when several properties overlap -- as they do in
# nearly every check in this list -- each mutation lands on whichever fires first and the narrow
# ones underneath accumulate controls that have never once executed them. The ladder paid for this:
# eleven invariants, mutation-tested one at a time, nine died and two survived; both survivors had
# passing tests whose fixtures some broader rule rejected first, so the rule under test never ran.
# Three phases of green over dead code. This runs each control with its probe blinded -- findings
# replaced by one shape-preserving constant that names nothing -- and a control that still passes
# is the finding. The corpus is discovered by scanning for `--negative-control` (LSN-036) and an
# empty corpus is a FAIL. Its own `--negative-control` scores six controls of known quality.
python3 dev/tests/negative-controls-name-their-rule.py
python3 dev/tests/negative-controls-name-their-rule.py --negative-control

# --- the L2 auth suite's own negative control (P9-T8b-4b-i) ---------------------------------
# The `¬` arm of V-BRK-007/008/009/010/017. It is here, in the chain that needs no cluster, because
# it deliberately addresses none: the only way to inject "a broker that accepts a credential it must
# refuse" would be to build and deploy a broker that does, so the injection point moves out to the
# transcript. Three hand-written transcripts of a misbehaving broker are replayed through the same
# assertion block the live run uses, and every one of the eight credential arms must go red on all
# three. The sharpest is `wrong-reason`: every negative IS refused, each with another arm's reason,
# which is exactly what a suite asserting "not 200" would pass. The live half of these five rows is
# in dev/L2-CHAIN.txt and needs the scratch cluster.
bash dev/verify/broker-auth-l2.sh --negative-control

# --- the auth can-i sweep's derivation, and its negative control (P9-T9b-5c) -----------------
# Two lines for one suite, because it has two failure modes and neither one detects the other.
#
# FIRST: does the question table derive anything, and does it derive the RIGHT SHAPE? The sweep asks
# the live authorizer only what this fixture computes, by importing dev/tests/actor-grant-single-
# sourced.py's own parser of 06 §2.2/§2.2.1 — one parser in the repo, so an L2/L0 disagreement can
# never be a spec-reading difference wearing a cluster defect's clothes. The failure that makes this
# line necessary is silent: a parser change that returns an empty set gives a sweep that asks
# nothing, answers nothing, and prints PROVEN. The self-test asserts row counts, three tiers, both
# directions populated, every question kind present, and that the fleetfreezes question is asked of
# every tier by name — V-BRK-013's sentence singles out developer-team, and a table that dropped it
# would still look like a table.
python3 dev/verify/fixtures/actor_grant_expectations.py --self-test

# SECOND: can the assertion block tell a correct sweep from a broken one? Fourteen transcripts, one
# correct and thirteen carrying a defect each, replayed through the same seven arms the live run
# uses — the arms are a file, actor_grant_sweep_assert.py, precisely so that the ¬ exercises the
# code the cluster run executes rather than a paraphrase of it. Each case names the arm that must
# catch it, so a transcript that goes red for some OTHER reason scores as a MISS, not a catch.
#
# The two sharpest are `rbac-never-applied`, where every question answers `no` — the exact shape of
# a fleet whose RBAC was never installed, and the exact shape a one-sided "no write verbs" sweep
# calls a perfect pass — and `reader-bound-to-nothing`, where an identity that can do nothing at all
# satisfies every negative row in the file.
#
# NEGATIVE CONTROL DOES NOT EXERCISE ([[LSN-060]]): the transcripts are synthesised, so identity
# discovery, the served-resource filter, the `kubectl auth can-i` invocation itself and the fixture
# seeding are all upstream of what this measures. The live half in dev/L2-CHAIN.txt covers those,
# and its on-cluster ¬ — an unlabelled Role granting a real actor a write verb — is the arm that
# proves the block can go red against an authorizer rather than against a file.
bash dev/verify/actor-grant-sweep-l2.sh --negative-control

# --- repository mechanics (LSN-012) ---------------------------------------------------------
# Which remote carries the work, and does `main` point at it? Local main tracked `upstream/main`
# (gke-labs) for the whole of phases 2-8: a bare `git pull` would have fetched a tree with none of
# this build in it. Resolved by CONTENT, never by remote name -- the name has already changed once.
bash dev/git-preflight.sh

# --- the invariants gate itself (P8-T6) -----------------------------------------------------
# Runs last: it reads this file, and several of its checks are about the chain above.
python3 dev/tests/invariants-gate.py

# --- the undo-coverage soak's envelope population (P9-T8b-4b-ii-2b-ii-a) ---------------------
# V-REV-001 is "100% of executed non-gated ActionRecords carry a validated undo plan", and a
# percentage is only as good as its denominator. Today's is ONE — broker-execute-l2.sh submits a
# single hand-written envelope and its results.csv note says n=1. This fixture derives the real
# population from verification/fixtures/classifier-corpus.yaml, filtered by what the shipped write
# overlay actually authorizes, subresources included.
#
# A DERIVED corpus fails the opposite way to a listed one: it shrinks to nothing silently and prints
# the empty result as a clean run. So the self-test carries floors (>=20 cases, >=3 verbs, >=2 kinds,
# both non-gated classes, both seed states, no target collisions, the reject histogram summing to
# the whole corpus) AND fires those floors against a synthesised empty selection, because a floor
# that has never been observed to fire is a floor whose threshold is a guess. Twelve more arms mutate
# the grant in memory — revoke `deployments` and every non-scale Deployment case must drop while the
# `deployments/scale` case stands; make the grant read-only and every previously-selected case must
# come back as `not-authorized`, by that name.
python3 dev/verify/fixtures/soak_corpus.py --self-test

# --- the undo-coverage soak's assertion block (P9-T8b-4b-ii-2b-ii-b) -------------------------
# The other half of the pair above: the corpus is the population, this is the suite that submits it
# and scores V-REV-001 over what comes back. Its live line is in dev/L2-CHAIN.txt.
#
# Seventeen synthesised populations replayed through assert_population, the same six arms the
# cluster run executes. Each case names the arm that must catch it and matches on that arm's own
# needle, so a population that goes red for some other reason scores MISS rather than a catch
# ([[LSN-035]]). The four V-REV-001 failure categories are separate cases -- `no-undo-plan`,
# `strategy-none-on-non-gated`, `unvalidated-plan`, `stepless-plan` -- because they are four
# different defects in the undo planner and a suite that reports them as one number cannot say
# which one it found.
#
# THE DEFER CASES ARE THE POINT. A soak whose population arrived empty, or fell below the accept
# floor, or covered a single verb, must not print a green 100%: 100% of zero is arithmetically
# perfect and evidentially nothing. Those three cases assert `DEFER:` and a named blocker, and the
# live run exits 3 on any of them ([[LSN-060]]'s sibling failure -- a suite that cannot run its
# property reports `deferred`, never `pass`).
#
# NEGATIVE CONTROL DOES NOT EXERCISE ([[LSN-060]]): the populations are synthesised, so everything
# upstream of the assertion block is untested here -- deriving the table from soak_corpus.py,
# base64ing it through BROKER_DRIVER_EXTRA_ENV, the probe's per-envelope nonce fetch and envelope
# construction, seeding the 37 targets and the two bulk reads that snapshot them, mining the journal
# and deriving `ar-<lowercase-actionId>`, and both P1 arms plus the broker Availability check. Only
# the live line proves those, and only against a cluster.
bash dev/verify/undo-coverage-l2.sh --negative-control

# --- The phase ratchet, derived rather than remembered (planning defect 4) ------------------------
# The CHECK ITSELF is not on this chain, and that is deliberate. `phase-ratchet-is-asserted.py
# --phase 9` is RED today by construction: 38 of Phase 9's 75 required check IDs have no green row
# yet. The chain is a required PR check, so an arm that stays red until an entire phase closes would
# redden every unrelated commit and destroy the per-commit attribution CHECKPOINT exists to produce
# ([[LSN-055]]). It runs in `dev/verify/verify-phase9.sh` section J, and `invariants-gate.py`
# (`check_phase_gate_runs_its_own_ratchet`, above) is what stops that section being removed.
#
# What IS chain-eligible is the control, which is green today and green after the phase closes. It
# exercises the real 09 §10 + phase-file derivation in every case -- only the answer table is varied
# ([[LSN-060]]) -- and one of its eight cases is the FUTURE tree, in which every required check is
# green and the arm must go GREEN ([[LSN-053]]). That case is the one that matters: an arm that
# could not go green once the work lands makes editing the arm the cheapest diff at exactly the
# wrong moment.
python3 dev/tests/phase-ratchet-is-asserted.py --phase 9 --negative-control

# --- V-MET-001: the catalog and the code agree about what is implemented --------------------------
# 09 §8's row is two directions between two sets -- every check ID in §6 exists in the implemented
# suite, and every implemented test declares a known check ID -- and until
# `verification/implementations.yaml` there was no artifact holding the second one.
#
# THE COST OF NOT HAVING IT. `phase-ratchet-is-asserted.py`'s `hint: named by ...` column is a
# `git grep <check-id>` under a footer disclaiming it in as many words. On 2026-07-31 it was read as
# coverage anyway and P9-T11g was scheduled promising eleven runs and two builds; the tree held one
# run and twelve builds. Nine of the eleven were "named by" a parser fixture, by binding.md and the
# skills that REQUIRE the check, or by VAP corpora that are real, correct, and executed by nothing.
# A grep cannot tell an assertion from a citation from a disclaimer, and a second grep will not
# either. The registry is a human's answer; this arm is what stops it drifting from the tree.
#
# The forward direction is deliberately PHASE-SCOPED to required-and-green. A check with no green
# row is the ratchet's population, not this one's -- giving one gap two gates gives it two chances
# to be answered differently.
python3 dev/tests/check-ids-have-implementations.py

# Eight mutations, each asserting the finding ITS property produces rather than that something went
# red ([[LSN-035]]): a registry row for an ID 09 §6 does not define, an `asserts_in` pointing at
# nothing, an implementation that stops naming its own ID, a `runs` on neither chain, a `runs` that
# does not reach the file it claims, a required-and-green check losing its row, a test declaring a
# renumbered ID, and a second `unimplemented:` row smuggled past the ceiling. The fixtures are read
# out of the tree (`_a_required_green`), not listed, so the control cannot go stale against it.
python3 dev/tests/check-ids-have-implementations.py --negative-control

# --- V-MET-009: the uncovered requirement list is published, not merely counted -------------------
# 09 §8 says "every normative requirement in 01-08 maps to at least one check ID", and until
# `verification/requirements.yaml` that sentence had no denominator in the tree. The file often
# mistaken for one, `verification/traceability.yaml`, maps the 177 bullets of the six spec
# Verification sections; that is V-MET-011's property over a different and much smaller population.
# So 09 §8.1's "roughly 45% uncovered" was a percentage with no list behind it -- the exact shape
# V-MET-009 exists to forbid.
#
# WHY BOTH BASELINES ARE RECORDED. §8.1's audit is reproducible for 02 (148 published, 147 by these
# rules) and is NOT uniformly reproducible: 78 for 05 against ~204, "96 (groups)" for 06 against
# ~374. That audit grouped some documents and enumerated others, so the spec's numbers and this
# enumeration are different populations. Both are recorded in `verification/coverage.yaml` -- the
# audit asserted cell-for-cell against the spec so it cannot drift, the enumeration's own count
# beside it -- because V-MET-008's ratchet needs a baseline in the units it measures in.
#
# `checks:` IS CURATED AND `--emit` MERGES. The enumeration is generated and must not be
# hand-edited; the mapping is a human's answer and regenerating never discards it.
python3 dev/tests/requirements-are-enumerated.py

# Eight mutations, each naming the finding ITS property produces ([[LSN-035]]): a spec statement
# left unenumerated, an enumerated one deleted, one silently reworded (the edit that would re-point
# a mapping at a sentence that is gone), the uncovered list replaced by its count, the list
# truncated, a requirement marked covered with no check behind it, the recorded §8.1 baseline edited
# away from the spec, and a section's ordinals made non-contiguous.
python3 dev/tests/requirements-are-enumerated.py --negative-control

# V-MET-008 -- the coverage ratchet, and the requirement-to-suite ownership both coverage gates
# stand on. Ownership is DERIVED from 09 §6's `Source` cells rather than declared, because
# V-MET-002 and V-MET-008 are complements of one partition and declaring a section unowned would
# shrink the BLOCKING-ALWAYS obligation and grow the lenient one. Also asserts §8.1's arrival
# clause over a content-digest baseline, so an inserted statement is detected by its text rather
# than by a positional ID every insertion above it shifts.
python3 dev/tests/coverage-ratchet-holds.py

# Eleven mutations, each naming the finding ITS property produces ([[LSN-035]]): an arrival with no
# check and no deferral, a deferral missing its owner, V-ISO's prose source deleted so a
# BLOCKING-ALWAYS suite owns zero SECTIONS, traceability.yaml's 05§8 bullets deleted so it owns one
# section and zero OBLIGATIONS (the same false green one level down), a Source cell blanked, a
# recorded ownership row deleted, the load-bearing count edited, V-MET-002's worklist truncated,
# the floor raised above the coverage under it, the enumerator returning almost nothing, and the
# arrival baseline emptied.
python3 dev/tests/coverage-ratchet-holds.py --negative-control
